# مراجعة 4 — تحديث فجوة feature flags بعد حذف المشاريع وإضافة RLS

> **تشخيص فقط — لم يُعدَّل أي كود أو قاعدة بيانات.**
> فحص طازج (grep كامل الآن، لا اعتماد على تقرير review-1 القديم) بتاريخ اليوم، بعد اكتمال كل دفعات RLS الـ15 وترحيل تسجيل الدخول لـ`login_with_pin`.

## ⚠️ قيد منهجي مهم قبل البدء
`projects` و`app_users` أصبحا محميين بـRLS منذ الدفعة الأخيرة — **لم أعد أستطيع قراءتهما مباشرة بمفتاح anon** (نفس آلية الحماية التي بنيناها بالضبط). الحقائق الخاصة بقاعدة البيانات أدناه (مزاهر المشروع الوحيد، بلا فروع، `modules=[]`) مبنية على ما تأكَّد **مراراً** بهذي الجلسة (جولة الحذف + كل دفعات RLS) لا على استعلام حي جديد. لو أردت تأكيداً حرفياً الآن، أرسل نتيجة:
```sql
SELECT id, name, subdomain FROM projects;
SELECT modules, settings FROM project_settings WHERE project_id = (SELECT id FROM projects WHERE name = 'ديوانية مزاهر');
```

---

## 1. الجرد الكامل المحدَّث — كل موضع، وحالته الآن

### 🟢 حيّ وذو صلة فعلية بمزاهر (4 مواضع)

| # | الملف : السطر | الشرط | ماذا يفعل |
|---|---|---|---|
| 1 | [Layout.jsx:39](src/components/Layout.jsx#L39) | `n === 'ديوانية مزاهر'` | يُظهر رابط "إدخال سريع للمبيعات" |
| 2 | [CashierDashboard.jsx:297](src/pages/CashierDashboard.jsx#L297) | `projectName === 'ديوانية مزاهر'` | بانر توجيه الكاشير لاستخدام الإدخال السريع |
| 3 | [PendingDocuments.jsx:543](src/pages/PendingDocuments.jsx#L543)¹ | `docProjName === 'محمصة كون'` | تحقق إلزامي (تصنيف + مصدر دفع) قبل الاعتماد |
| 4 | [financialEngine.js:20](src/lib/financialEngine.js#L20)² | (ليس شرط مشروع — بيانات مزروعة) | أسماء شركاء مزاهر الحقيقيين (`مسحوبات سليمان/فايز/أم طوبى`) |

¹ **مُدرَج هنا رغم أن شرطه أصبح ميتاً حرفياً (محمصة كون محذوفة) — لأن الوظيفة التي يحرسها ما زالت مطلوبة لمزاهر نفسها.** هذا أهم موضع بكل القائمة — تفصيله بالقسم التالي.
² خارج نطاق "شروط أسماء المشاريع" الذي طلبته تحديداً (هذا حقل بيانات لا `if (projectName === ...)`)، لكنه من نفس عائلة المشكلة (تخصيص مزاهر مزروع بكود عام) فأذكره للسياق فقط — لن يدخل بخطة التنفيذ أدناه لأنه خارج الطلب المحدَّد.

### ⚫ ميت تماماً الآن (لم يكن كذلك بـreview-1) — 18 موضعاً

كل هذي مقيَّدة بأسماء `تشورميك`/`محمصة كون`/`بـ عسل`/`مطعم الوادي` — **الأربعة محذوفون فعلياً**، فلا يوجد أي مشروع بقاعدة البيانات اليوم يمكن أن يُطابق أياً من هذي الشروط:

| # | الملف : السطر | الشرط |
|---|---|---|
| 5 | [Layout.jsx:41](src/components/Layout.jsx#L41) | `n === 'محمصة كون'` |
| 6 | [Layout.jsx:48](src/components/Layout.jsx#L48) | `includes('بـ عسل')` |
| 7 | [Layout.jsx:49](src/components/Layout.jsx#L49) | `includes('بـ عسل')` |
| 8 | [CashierDashboard.jsx:210](src/pages/CashierDashboard.jsx#L210) | `branch === 'المحمصة الرئيسية'` (بلا فروع بمزاهر أصلاً) |
| 9 | [InvoiceUpload.jsx:40](src/pages/InvoiceUpload.jsx#L40) | `includes('بـ عسل')` |
| 10 | [PendingDocuments.jsx:71](src/pages/PendingDocuments.jsx#L71) | `includes('تشورميك')` |
| 11 | [PendingDocuments.jsx:74](src/pages/PendingDocuments.jsx#L74) | `includes('كون')` ⚠️ نمط خطر (يطابق أي اسم فيه "كون") |
| 12 | [PendingDocuments.jsx:75](src/pages/PendingDocuments.jsx#L75) | نفس السطر أعلاه، دور accountant |
| 13 | [PendingDocuments.jsx:246](src/pages/PendingDocuments.jsx#L246) | `isTashormik = includes('تشورميك')` |
| 14 | [PendingDocuments.jsx:312](src/pages/PendingDocuments.jsx#L312) | `includes('تشورميك') ? 'bank' : ...` |
| 15 | [PendingDocuments.jsx:828](src/pages/PendingDocuments.jsx#L828) | `hideBranchPicker = includes('بـ عسل')` |
| 16 | [Reports.jsx:97](src/pages/Reports.jsx#L97) | `isBaAsal = includes('بـ عسل')` |
| 17 | [Sales.jsx:323](src/pages/Sales.jsx#L323) | `isMahmasa = projectName === 'محمصة كون'` |
| 18 | [Dashboard.jsx:685](src/pages/Dashboard.jsx#L685) | `includes('بـ عسل')` |
| 19 | [claude.js:260](src/lib/claude.js#L260) | `projectName === 'تشورميك'` |
| 20 | [AppReconciliation.jsx:11](src/pages/AppReconciliation.jsx#L11) | `TARGET_PROJECT = 'بـ عسل'` (يقفل الصفحة كلها) |
| 21 | [PayableSuppliers.jsx:9](src/pages/PayableSuppliers.jsx#L9) | نفس `TARGET_PROJECT` |
| 22 | [RoasterySales.jsx:8](src/pages/RoasterySales.jsx#L8) | `BRANCH = 'المحمصة الرئيسية'` (الصفحة كلها بلا معنى بلا الفرع) |
| 23–27 | [Login.jsx:12-38](src/pages/Login.jsx#L12) | خريطة `PROJECT_INFO` — 4 من 5 مدخلات (`tashormik`,`koon`,`trial`,`basal`) **ميتة وظيفياً فقط** (تظهر واجهة ترحيب لمشروع لا يمكن الدخول له فعلياً — تسجيل الدخول سيفشل حتماً عبر `login_with_pin` لأن subdomain لا يطابق أي `projects.subdomain` حي) |

### 🔴 الأخطر بالقائمة كلها — كان "حي" بـreview-1، صار "ميتاً" الآن بأثر جانبي خطير

**الموضع #3 أعلاه ([PendingDocuments.jsx:543](src/pages/PendingDocuments.jsx#L543)):**
```js
const isMahmasa = docProjName === 'محمصة كون'
if (isMahmasa && res?.type !== 'sales' && res?.type !== 'transfer') {
  // ... يمنع الاعتماد لو نقص التصنيف أو مصدر الدفع
}
```
بـ`review-1` كان هذا "حياً لمحمصة كون فقط" وأوصيت وقتها بتعميمه لا حذفه. **الآن بعد الحذف، `isMahmasa` لا يمكن أن يكون `true` أبداً — أي أن هذا التحقق الإلزامي معطَّل بالكامل لكل المشاريع، بما فيها مزاهر، منذ لحظة حذف محمصة كون** (قبل أي عمل بجولة RLS اليوم). **لا يوجد أي حارس اليوم يمنع اعتماد مصروف بلا تصنيف أو مصدر دفع لمزاهر.** هذا يستحق أولوية قصوى — أعلى حتى من تنظيف الكود الميت.

---

## 2. التصنيف — ثلاث فئات لا فئتان (أدق من "خاص/عام")

طلبت تصنيفاً ثنائياً (يبقى خاصاً بمزاهر / كان يجب أن يكون افتراضياً) — الفحص الفعلي كشف **فئة ثالثة ضرورية**:

| الفئة | المعنى | المواضع |
|---|---|---|
| **أ) علم ميزة حقيقي** (Feature Flag) | سلوك اختياري بطبيعته — بعض المشاريع تريده، بعضها لا | #1، #2 (الإدخال السريع) |
| **ب) قاعدة عامة يجب أن تُطبَّق افتراضياً** | ليست خاصية اختيارية أصلاً — كانت خطأ تنفيذ حصرها بمشروع واحد | **#3 (التحقق الإلزامي)** — الأهم |
| **جـ) ميت تماماً — لا يستحق تحويله لعلم إطلاقاً** | كان خاصاً بمشروع محذوف نهائياً، لا مزاهر تريده ولا أي مشروع مستقبلي قريب يحتاجه بالضرورة | كل الـ23 موضعاً المتبقية (#5-27) |

**لماذا الفئة (جـ) لا تحتاج "تصميم علم" أصلاً:** تحويل "دعم آجل لمورد بـ عسل" أو "مطابقة كشوف بنكية بصيغة تشورميك" لعلم `project_settings.features.x = false` افتراضياً **حل مبالغ فيه** لكود لا يخدم أي مستخدم حالي أو معروف مستقبلاً — الأصح حذفه، وإعادة بنائه بشكل نظيف (مع علم فعلي) **حين** يظهر عميل جديد يحتاج فعلاً ميزة الموردين مثلاً، لا قبل ذلك بتخمين الشكل الصحيح للعلم مسبقاً.

---

## 3. تصميم `project_settings` كنظام feature flags — يبني على ما هو موجود وصحيح أصلاً

اكتشفت بمراجعة سابقة (review-1) أن `Layout.jsx` يحتوي **نمطاً صحيحاً بالفعل** غير مستغَل إلا مرة واحدة:
```js
{ to: '/suppliers', ..., module: 'suppliers' },   // ✅ يتحقق من modules.includes('suppliers')
```
`project_settings.modules` (عمود `text[]` موجود أصلاً) + `AuthContext` يحمّله فعلاً بـ`getProjectModules()` ويوفّره بكل الصفحات عبر `useAuth().modules`. **لا حاجة لبنية جديدة — فقط توسيع استخدام الموجود:**

### أ) الأعلام البسيطة (تشغيل/إيقاف رابط أو سلوك) → أضِف لـ`modules` الموجود
```
modules: ['quick_sale']   // لمزاهر تحديداً — بدل cond: n => n === 'ديوانية مزاهر'
```
يغطي: #1، #2 (الإدخال السريع). كل مشروع جديد يبدأ بـ`modules: []` (كحاله اليوم فعلياً).

### ب) القاعدة العامة (الفئة ب) → **لا تحتاج أي علم إطلاقاً**
`isMahmasa` يُحذف كلياً، والتحقق يصبح غير مشروط:
```js
if (res?.type !== 'sales' && res?.type !== 'transfer') {
  // نفس منطق التحقق، بلا أي شرط مشروع
}
```
لو ظهر مستقبلاً مشروع يحتاج استثناءً حقيقياً (نادر ومحدَّد)، عندها فقط يُضاف علم صريح بعكس الاتجاه: `settings.accounting.skip_mandatory_validation: false` (استثناء صريح، لا الوضع الافتراضي).

### جـ) إعدادات بيانات (لا أعلام تشغيل/إيقاف) — لو أُعيد بناء ميزة من الفئة (جـ) لاحقاً لعميل جديد
```jsonc
"settings": {
  "transaction_types": [...],   // موجود
  "branches": null,             // موجود

  "defaults": {
    "forced_pay_source": null,          // بديل شرط "تشورميك" — يُضبط لكل مشروع يحتاجه
    "pay_source_by_role": { "cashier": "cash", "purchasing": "custody", "accountant": "bank", "owner": "bank" }
  }
}
```

**لن أصمّم بنية للفئة (جـ) بالتفصيل الآن** (الموردين، مطابقة التطبيقات، الذمم) — تصميمها اليوم تخمين لشكل لا نعرفه إلا حين يظهر العميل الفعلي الذي يحتاجها.

---

## 4. خطة التنفيذ التدريجية

بنفس أسلوب دفعات RLS — **لكن الحجم مختلف جوهرياً هنا**: 23 من 27 موضعاً كود ميت 100% (صفر مخاطرة حذف، لأن لا بيانات تُطابقه إطلاقاً)، لا يحتاج نفس الحذر التدريجي البطيء الذي استحقته RLS. اقترح 3 دفعات فقط:

| الدفعة | ماذا | المخاطرة | الاختبار |
|---|---|---|---|
| **1 — الأعلى أولوية، منفصلة عن الباقي** | إصلاح #3: حذف شرط `isMahmasa`، تعميم التحقق الإلزامي على كل المشاريع | ⚠️ **الوحيدة التي تغيّر سلوكاً فعلياً لمزاهر** (تحقق كان معطَّلاً يصبح فعّالاً) | accountant يحاول اعتماد مصروف بلا تصنيف/مصدر دفع → يجب أن يُرفض الآن (لم يكن يُرفض قبل الإصلاح) |
| **2 — حذف الكود الميت** | إزالة المواضع #5-27 كاملة: صفحتا `AppReconciliation.jsx`/`PayableSuppliers.jsx`، `RoasterySales.jsx`، فرع Mahmasa بـ`Sales.jsx`، فرع الفرع بـ`CashierDashboard.jsx`، شروط تشورميك/كون المتفرقة، تنظيف `PROJECT_INFO` لـ4 مدخلات ميتة | **صفر** — لا بيانات تُطابق أياً منها اليوم، حذفها لا يغيّر أي سلوك ظاهر لمزاهر | تصفّح كامل لكل صفحات مزاهر بكل الأدوار الخمسة، تأكيد عدم اختفاء أي شيء كان يعمل |
| **3 — الأعلام الحقيقية** | نقل #1/#2 (الإدخال السريع) لـ`modules` — يحتاج **تحديث صف واحد بقاعدة البيانات** (`UPDATE project_settings SET modules = array_append(modules, 'quick_sale') WHERE project_id = '<مزاهر>'`) + استبدال الشرطين بـ`module: 'quick_sale'`/`modules.includes('quick_sale')` | منخفضة، لكن **يحتاج ترتيب دقيق مثل دفعة app_users/projects بالضبط**: تحديث البيانات أولاً، ثم نشر الكود فوراً — لو انعكس الترتيب، الرابط يختفي مؤقتاً لمزاهر | كاشير يتأكد ظهور رابط "الإدخال السريع" والبانر بعد النشر مباشرة |

**التوصية: ابدأ بالدفعة 1 وحدها اليوم** — هي الوحيدة ذات القيمة الأمنية/المحاسبية الحقيقية والعاجلة (حارس محاسبي معطَّل بصمت). الدفعتان 2 و3 تنظيف معماري بلا إلحاح، يمكن تأجيلهما لجولة منفصلة بلا ضغط وقت.

**لم يُعدَّل أي كود أو قاعدة بيانات أثناء إعداد هذا التقرير.**
