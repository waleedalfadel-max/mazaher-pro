# المرحلة 2-أ — جلسة Supabase حقيقية خلف PIN (بلا أي تغيير بالشاشة)

> **تخطيط فقط. لا كود يُكتب، لا SQL يُنفَّذ.** هذه الخطوة التحضيرية الوحيدة التي تفتح الباب لتفعيل RLS على الـ11 جدولاً "العاديين" (`plan-rls-phase2.md`، القسم 2-3 حتى 2-13) دون تعطيل accountant/cashier/purchasing/superadmin.

## السياق والهدف بدقة
بعد المرحلة 1: `owner` فقط له `auth.uid()` حقيقي (جلسة Supabase عبر بريد/كلمة مرور). البقية (accountant, cashier, purchasing, superadmin) يدخلون بـPIN فقط — طلباتهم تصل بدور Postgres `anon`، و`auth.uid()` يُرجع `NULL` دائماً لهذا الدور (سلوك Postgres قطعي، لا احتمال). أي سياسة RLS تعتمد `auth.uid()` تحجب هؤلاء الأربعة فوراً.

**الهدف:** بعد إدخال PIN بنجاح، تحصل الجلسة تلقائياً على جلسة Supabase حقيقية أيضاً — **بلا أي حقل جديد بالشاشة، بلا أي شيء يكتبه المستخدم إضافياً.** شاشة الدخول والـPIN بالضبط كما هما اليوم.

---

## 1. الآلية المختارة — ولماذا لا الأسهل ظاهرياً

قيّمت خيارين تقنيين قبل الترشيح:

| الآلية | الوصف | لماذا رُفضت/رُشّحت |
|---|---|---|
| **JWT مصنوع يدوياً** بمفتاح `SUPABASE_JWT_SECRET` (بلا حساب `auth.users` فعلي) | الخادم يوقّع توكن بنفسه بـ`sub = app_users.id` | ❌ **مرفوض.** `supabase-js` على العميل يدير تجديد الجلسة (`refresh_token`) تلقائياً بافتراض أن التوكن صادر من نظام Auth الحقيقي. توكن مصنوع يدوياً لا يملك `refresh_token` مطابقاً بجداول Supabase الداخلية — يعمل حتى انتهاء صلاحيته (عادة ساعة) ثم يفشل التجديد **بصمت**، فتصير طلبات RLS تُرفض بلا أي خطأ ظاهر للمستخدم بعد ساعة من الجلسة المفتوحة. هشاشة غير مقبولة لتطبيق يُستخدم يومياً بجلسات طويلة. |
| **حساب Supabase Auth حقيقي + رابط سحري (magic link) يُصدره الخادم** | الخادم (بمفتاح `service_role`) يُصدر حساباً اصطناعياً لكل صف `app_users`، ثم عند كل دخول PIN ناجح يولّد رابط دخول (`admin.generateLink`) ويُسلّمه للعميل ليُبادله بجلسة حقيقية (`auth.verifyOtp`) | ✅ **المُرشَّح.** جلسة حقيقية بالكامل — Supabase يدير التجديد تلقائياً بلا هشاشة. لا يحتاج اختراع أو تخزين أي "كلمة مرور" اصطناعية لكل مستخدم (خلافاً لبديل ثالث فكّرت فيه: كلمة مرور مشتقة من دالة تجزئة — رفضته لأنه يخترع نمط اعتماد غير قياسي بلا فائدة حقيقية على الطريقة المدعومة رسمياً). |

**الآلية المرشَّحة تفصيلياً:**
1. المستخدم يدخل PIN كما اليوم بالضبط. `AuthContext.login(pin)` يطابقه محلياً بنفس المنطق الحالي **بلا أي تغيير**.
2. عند النجاح، **خطوة إضافية جديدة** (لا تستبدل شيئاً): الواجهة تستدعي نقطة نهاية خادمية جديدة `POST /api/pin-session` بـ`{ userId, pin }` (معرّف الصف المطابَق + نفس الـPIN).
3. الخادم (بمفتاح `service_role` — لا يغادر الخادم إطلاقاً):
   - **يعيد التحقق من PIN بنفسه** — لا يثق بمطابقة العميل وحدها (دفاع بعمق، ويصير هذا أيضاً مكان تطبيق حد المحاولات الفعلي، القسم 4).
   - لو `app_users.auth_id` فارغ (أول دخول لهذا الصف بعد هذي المرحلة): ينشئ حساب Supabase Auth ببريد اصطناعي مشتق من `app_users.id` (مثل `u-<uuid>@pin.internal.tahseeb.app` — ليس بريداً حقيقياً، لا يُرسَل له شيء أبداً)، ويحدّث `app_users.auth_id` بمعرّف الحساب الجديد.
   - يستدعي `admin.generateLink({ type: 'magiclink', email })` ويُعيد للعميل رمز التبادل (`token_hash`/`email_otp` حسب حقول الاستجابة بإصدار `@supabase/supabase-js@2.45` المُثبَّت حالياً — يُؤكَّد الاسم الدقيق عند التنفيذ).
4. العميل يستدعي `supabase.auth.verifyOtp({ email, token_hash, type: 'magiclink' })` — يُنشئ جلسة Supabase حقيقية (access + refresh token) تُدار تلقائياً من الآن فصاعداً بنفس آلية جلسة المالك بالبريد.
5. بقية `AuthContext` (حالة `role`/`projectId`/`sessionStorage`) **لا تتغيّر إطلاقاً** — هذي الخطوة تضيف طبقة هوية Supabase تحتية فقط، فوقها الحالة الظاهرة للتطبيق كما هي.

**لا حاجة لأي عمود DB جديد** — `app_users.auth_id` موجود أصلاً من المرحلة 1 (nullable، unique)، ويُستخدم الآن لكل الأدوار بدل المالك فقط.

---

## 2. الفشل الآمن — أهم خاصية تصميمية

الخطوة 2-4 أعلاه **إضافية بحتة وغير حاجبة (best-effort)**: تُغلَّف بـ`try/catch`. لو فشلت (شبكة، خطأ خادم، أي شيء)، **تسجيل الدخول ينجح كما اليوم بالضبط** — `role`/`projectId`/`sessionStorage` تُضبط، والمستخدم يدخل. الفارق الوحيد: تلك الجلسة تبقى بلا `auth.uid()` حقيقي (كحالها الحالية تماماً)، فتتعطّل فقط الصفحات المحمية بـRLS **إن كانت مفعّلة** — ولا شيء منها مفعَّل بعد.

**لماذا هذا حاسم:** يعني عطلاً بهذا الكود الجديد **لا يمكنه أبداً منع أي مستخدم من الدخول** — أسوأ سيناريو هو الرجوع لوضع اليوم بالضبط لتلك الجلسة، لا كسر جديد.

⚠️ **لكن قبل البدء بتفعيل RLS على الـ11 جدولاً (الدفعة 3 بـ`plan-rls-phase2.md`)، يجب أن يكون منح `auth.uid()` **موثوقاً لا "أفضل جهد"** — التحقق بالقسم 6 يشترط نجاحاً متكرراً لكل دور قبل اعتبار هذي المرحلة مكتملة، تحديداً لهذا السبب.

---

## 3. الملفات المتأثرة (لم تُنشأ بعد)

| الملف | الحالة | الوصف |
|---|---|---|
| `api/_supabaseAdmin.js` | 🆕 جديد | عميل Supabase بمفتاح `service_role` — **يُستورَد من `api/` فقط، لا من `src/` إطلاقاً** (لو استُورد بالواجهة، ينكسر البناء عبر Vite لأن المتغيّر غير مسبوق بـ`VITE_` أصلاً — حماية إضافية غير مقصودة لكنها مفيدة) |
| `api/pin-session.js` | 🆕 جديد | نقطة النهاية. يستورد `checkOrigin` من `api/_guard.js` مباشرة (نفس الآلية المستخدمة بـ`api/analyze.js`) + حد محاولات مخصص (القسم 4) |
| `src/contexts/AuthContext.jsx` | ✏️ تعديل إضافي فقط | داخل `login(pin)`، بعد نجاح المطابقة الحالية (لا قبلها ولا بدلاً منها): استدعاء `/api/pin-session` ثم `supabase.auth.verifyOtp(...)`، بكامله داخل `try/catch` لا يمنع الإرجاع الطبيعي لـ`user.role` |
| `src/pages/Login.jsx` | ❌ بلا تغيير | لا علاقة له بهذي الآلية إطلاقاً |
| قاعدة البيانات | ❌ بلا تغيير | العمود موجود أصلاً؛ لا `ALTER`/`CREATE POLICY` بهذي المرحلة |

**متغيّرات بيئة جديدة (Vercel، خادمية فقط، لا `VITE_`):**
- `SUPABASE_SERVICE_ROLE_KEY` — من لوحة Supabase → Settings → API → **service_role (secret)**. ⚠️ هذا المفتاح **يتجاوز RLS بالكامل** — تسريبه أخطر من كل ما نحاول إصلاحه. يُضاف بلوحة Vercel مباشرة من قِبلك، لن أطلبه ولن أتعامل معه، تماماً كما لم أتعامل مع كلمة مرورك بالمرحلة 1.

---

## 4. حماية نقطة النهاية الجديدة — لا تصير ثغرة جديدة

`/api/pin-session` يستقبل PIN فعلياً — يجب ألا يصير أداة تخمين آلية أسرع من قفل الواجهة الحالي (`Login.jsx`: 3 محاولات / قفل 5 دقائق، لكنه **من جهة العميل فقط** — قابل للتجاوز بنداء مباشر للـAPI).

**الحماية المطلوبة داخل `pin-session.js`:**
1. `checkOrigin` من `_guard.js` — استيراد مباشر، بلا تكرار (نفس نمط `api/analyze.js`).
2. **حد محاولات خادمي مخصص** — أشدّ من محدِّد `checkRateLimit` العام (20/دقيقة مصمَّم لاستدعاءات Claude، ليس لمساحة PIN الصغيرة نسبياً — 5 أرقام = 100,000 احتمال). أقترح مطابقة أرقام `Login.jsx` نفسها للاتساق: **3 محاولات فاشلة لكل IP خلال 5 دقائق** ثم `429`، بنفس بنية `Map` داخل الذاكرة الموجودة أصلاً بـ`_guard.js` (نفس القيد المعروف: لا يُشارَك بين نسخ Vercel، يوقف الاندفاع من مصدر واحد فقط — مقبول هنا لأن الحماية الأساسية هي إعادة التحقق من الـPIN نفسه، لا العدّاد وحده).
3. **لا تُسجَّل قيمة PIN أبداً بأي `console.log`** — حتى بمسارات الخطأ.

---

## 5. التسلسل المقترح للتنفيذ (بعد موافقتك على هذي الخطة)

| الخطوة | ماذا |
|---|---|
| 1 | إنشاء `api/_supabaseAdmin.js` + `api/pin-session.js` (بلا لمس `AuthContext`) |
| 2 | اختبار نقطة النهاية بمعزل (سكربت Node مؤقت يحاكي طلباً بـPIN حقيقي لصف تجريبي — قراءة/كتابة عبر service_role مباشرة، بلا مرور بالواجهة) |
| 3 | تعديل `AuthContext.login()` بالخطوة الإضافية، مغلّفة بـ`try/catch` |
| 4 | بناء + نشر |
| 5 | اختبار حي بكل الأدوار الأربعة (القسم 6) |

---

## 6. خطة الاختبار — شرط الإتمام صريح

**قبل إعلان هذي المرحلة مكتملة (شرط لازم لبدء تفعيل RLS على الـ11 جدولاً):**

1. `npm run build` بلا أخطاء.
2. تسجيل دخول بـPIN لكل دور من الأربعة (accountant, cashier, purchasing, superadmin) — **3 مرات متتالية لكل دور** (لا مرة واحدة) للتأكد أن إنشاء الحساب أول مرة والتبادل بعدها يعملان بثبات، لا "نجح صدفة".
3. بعد كل دخول: تحقق قراءة فقط (سكربت Node بـservice_role، أو استعلام SQL بلوحة Supabase) أن `app_users.auth_id` للصف صار غير فارغ، وأنه يطابق حساباً حقيقياً بـ`auth.users`.
4. **الاختبار الحاسم:** بجلسة كل دور، نفّذ استعلاماً تجريبياً يعتمد `auth.uid()` (مثل `SELECT auth.uid();` عبر الجلسة الفعلية بالمتصفح — أو مؤقتاً استخدام `auth_project_id()` من `plan-rls-phase2.md` إن كانت الدوال المساعدة قد أُنشئت بالدفعة 1) للتأكد أنه **لا يُرجع NULL** لأي من الأدوار الأربعة.
5. تأكيد عدم كسر أي شيء موجود: PIN pad بلا تغيير بصري، قفل 3 محاولات بالواجهة لا يزال يعمل، جلسة المالك بالبريد (المرحلة 1) وبـPIN كشبكة أمان — كلاهما بلا تأثّر.
6. مراقبة سجلات Vercel لأي فشل متكرر بـ`/api/pin-session` (يدل على مشكلة بالآلية تحتاج إصلاحاً قبل المتابعة، حتى لو الدخول نفسه لم ينكسر بفضل الفشل الآمن بالقسم 2).

**لا تبدأ الدفعة 3 من `plan-rls-phase2.md` (تفعيل RLS على الـ11 جدولاً) حتى تنجح الفقرة 4 أعلاه بثبات لكل الأدوار الأربعة.**

---

## 7. التراجع

- كود بحت — `git revert` للـcommit يزيل كل شيء فوراً.
- حتى بلا تراجع: تعطيل `/api/pin-session` (حذف الملف أو جعله يُعيد خطأ فوراً) يُرجِع كل الأدوار لوضع "PIN يعمل بلا `auth.uid()`" — بالضبط حالتها اليوم، بفضل الفشل الآمن بالقسم 2.
- `app_users.auth_id` المملوءة لأدوار غير المالك لا تحتاج تنظيفاً حتى لو تراجعنا — أعمدة فارغة أو ممتلئة لا تؤثر على أي منطق حالي (لا RLS مفعّل عليها بعد).

**لم يُنفَّذ أي كود أو تغيير أثناء إعداد هذي الخطة.**
