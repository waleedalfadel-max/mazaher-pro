# مراجعة 3 — تأمين الحالي مقابل إعادة البناء من الصفر

> **تقييم تشخيصي — لم يُعدَّل أي كود ولا قاعدة بيانات.** كل رقم أدناه من قياس فعلي على المستودع وقاعدة البيانات الحية بتاريخ 2026-08-11 (commit `ca998b5`).

---

## ⚡ الخلاصة أولاً (للقرار السريع)

**التوصية القاطعة: تأمين النظام الحالي في مكانه (الخيار أ). البناء من الصفر خطأ استراتيجي هنا، والأدلة قاطعة.**

السبب بجملة: **الثغرة الأمنية كلها في طبقة واحدة رقيقة (المصادقة + RLS)، بينما النضج المتراكم كله في طبقة أخرى منفصلة تماماً (المنطق المالي) لا علاقة لها بالثغرة.** إعادة البناء تهدم الطبقة الناضجة لتصلح طبقة لا تحتاج هدماً — تحتاج **إضافة**.

الأرقام التي تحسم:
- **48 من 115 commit** (42%) إصلاحات أخطاء مالية/محاسبية متراكمة عبر **~10 أسابيع**.
- طبقة المنطق المالي: **1,124 سطر في 12 ملف، 10 منها خالية تماماً من أي استعلام قاعدة بيانات** — تُنقل أو تبقى بلا أي تعديل في كلا الخيارين.
- تأمين الحالي يحتاج **15 سياسة RLS + دالة مصادقة واحدة**، ولا يمسّ أياً من الـ10,173 سطر منطق الواجهة.

---

## 0. حجم الثغرة — مُقاس فعلياً (ليس تقديراً)

شغّلت anon key (نفس مفتاح الواجهة المكشوف في حزمة JS) ضد قاعدة البيانات الحية:

| الاختبار | النتيجة |
|---|---|
| جداول مقروءة بالكامل بلا مصادقة | **15 / 15** |
| جداول محمية بـRLS | **0 / 15** |
| عمود `pin` مكشوف | ✅ **نعم — كل الأدوار الخمسة** (superadmin, owner, accountant, purchasing, cashier) |
| anon يستطيع الكتابة (INSERT/UPDATE/DELETE) | ✅ **نعم** (رُفض إدراجي التجريبي بخطأ *بنيوي* `null value in date` — لا بخطأ صلاحية، أي أن الصلاحية مفتوحة) |

**الخطورة الحقيقية أعمق مما ذُكر في السياق:** ليست "قراءة فقط". أي زائر يستطيع **حذف كل قيود مزاهر** أو **تعديل المبالغ** بسطر واحد، لأن `anon` يملك صلاحية الكتابة الكاملة. هذا يرفع الإلحاح، لكنه **لا يغيّر التوصية** — بل يعزّزها، لأن الحل (RLS) هو نفسه للقراءة والكتابة.

---

## 1. حجم تأمين النظام الحالي (الخيار أ)

### 1-أ. كم جدولاً يحتاج RLS؟

**15 جدولاً بالضبط.** كلها موجودة الآن، وكلها تحمل عمود `project_id` عدا استثناء واحد:

| الجدول | له `project_id`؟ | نمط سياسة RLS |
|---|:---:|---|
| `ledger_entries` · `documents` · `document_items` · `sales` · `categories` · `project_settings` · `suppliers` · `supplier_transactions` · `payable_suppliers` · `quick_sales_draft` · `loans` · `branches` · `attachments` (13 جدول) | ✅ | **نمط واحد متطابق:** `project_id = auth.jwt()->>'project_id'` |
| `projects` | العمود هو `id` | `id = auth.jwt()->>'project_id'` |
| `app_users` | ✅ | خاص — يقرأ صفّه فقط، والمحاسب يقرأ مستخدمي مشروعه |

**الأثر الحاسم:** 13 من 15 جدولاً تأخذ **نفس السياسة حرفياً**. هذا ليس 15 تصميماً مختلفاً — بل قالب واحد يُكرّر. لأن كل الجداول مُنظَّمة أصلاً حول `project_id`، البنية **متوافقة تماماً مع RLS بلا أي إعادة هيكلة للمخطط**.

### 1-ب. كم استعلاماً يعتمد على anon key ويحتاج إعادة ربط؟

| المقياس | العدد |
|---|---:|
| إجمالي استدعاءات Supabase من الواجهة | **115** |
| منها `SELECT` (عبر `.from`) | 108 |
| `.insert` | 33 · `.update` 19 · `.delete` 10 · `.rpc` 2 |
| استعلامات تفلتر يدوياً بـ`eq('project_id', ...)` | **58** |

**النقطة الحاسمة — وهي لصالح الخيار أ بقوة:**

عند إضافة Supabase Auth، **لا يحتاج أيٌّ من الـ115 استعلاماً إعادة كتابة لمنطقه**. السبب: مكتبة `supabase-js` نفسها تحمل رمز الجلسة تلقائياً في كل طلب بمجرد `signIn`. الكود الحالي:

```js
export const supabase = createClient(SUPABASE_URL, SUPABASE_KEY)  // supabase.js — 6 أسطر
```
يصبح نفسه — لكن الطلبات تُرسَل الآن مع JWT موقّع، وRLS يفلتر بناءً عليه في الخادم.

**الأفضل من ذلك:** الـ58 استعلاماً التي تفلتر يدوياً بـ`project_id` تصبح **حزام أمان مزدوجاً**: الفلتر اليدوي في الواجهة + RLS في الخادم. لا تحتاج حذفها (تبقى تحسيناً للأداء)، ولا تكسر شيئاً.

**ما يحتاج فعلاً إلى تعديل:** فقط الأماكن التي يعتمد فيها المنطق على `project_id` القادم من `sessionStorage` (المتلاعَب به) — يجب أن يأتي من الـJWT. لكن هذا تغيير في **مصدر** القيمة لا في **الاستعلامات**.

### 1-ج. نظام الدخول الحالي (PIN) — حجم إعادة الكتابة

المصادقة الحالية كلها في **ملف واحد**: [AuthContext.jsx](src/contexts/AuthContext.jsx) (165 سطر). قِستُ اعتماديتها:

```
grep "supabase.auth\|auth.uid\|getUser\|signIn" src/  →  صفر نتيجة
```

**لا يوجد أي استخدام لـSupabase Auth في المشروع كله إطلاقاً.** المصادقة الحالية:
- تقارن PIN نصاً ضد `app_users.pin` في المتصفح ([AuthContext.jsx:54-93](src/contexts/AuthContext.jsx#L54))
- تخزّن الهوية في `sessionStorage` (6 مفاتيح: `mz_role`, `mz_pid`, …) — **بلا أي رمز موقّع**
- `project_id` يُقرأ من `sessionStorage` ([AuthContext.jsx:44](src/contexts/AuthContext.jsx#L44)) — قابل للتلاعب الكامل من أدوات المطوّر

**حجم الربط بـSupabase Auth:**

| ما يتغيّر | الملفات الممسوسة | الحجم |
|---|---|---|
| منطق تسجيل الدخول (PIN → جلسة موقّعة) | `AuthContext.jsx` فقط | ملف واحد، ~80 سطر |
| مصدر `role`/`project_id`/`branch` (من sessionStorage → JWT claims) | `AuthContext.jsx` فقط | نفس الملف |
| كل الصفحات الـ20 التي تستهلك `useAuth()` | **صفر تغيير** | تبقى `const { projectId } = useAuth()` كما هي |

هذا هو جمال العزل: **كل الصفحات تقرأ الهوية عبر `useAuth()` من نقطة واحدة.** غيّر مصدرها في `AuthContext` فقط، وكل شيء فوقها يبقى سليماً.

**التحدي الوحيد الحقيقي:** PIN ليس بريد/كلمة مرور. الحل المعتاد — دالة خادمية (Edge Function أو `api/`) تتحقق من PIN وتُصدر جلسة عبر `supabase.auth.admin` — أو استخدام PIN كـpassword مع بريد مُصطنَع `{role}@{project}.internal`. أياً كان، **التغيير محصور في `AuthContext.jsx` + دالة خادمية واحدة جديدة.**

### 1-د. هل البنية متوافقة مع RLS؟ — **نعم، بدرجة عالية**

| عامل التوافق | الحالة | الأثر |
|---|:---:|---|
| كل جدول منظّم حول `project_id` | ✅ | سياسة RLS = قالب واحد |
| الهوية مركزية في `useAuth()` | ✅ | نقطة تغيير واحدة |
| الاستعلامات مباشرة من الواجهة (لا backend وسيط) | ⚠️ محايد | RLS **مصمَّم بالضبط لهذا** — الحماية في الخادم لا الوسيط |
| superadmin يصل لكل المشاريع | ⚠️ يحتاج سياسة خاصة | claim `role=superadmin` في JWT يتجاوز فلتر project_id |

**نقطة الانتباه الوحيدة (superadmin):** [SuperAdmin.jsx](src/pages/SuperAdmin.jsx) و[PendingDocuments.jsx](src/pages/PendingDocuments.jsx) يستعلمان عبر كل المشاريع (لوحة مزوّد الخدمة). سياسة RLS يجب أن تسمح: `project_id = jwt.project_id OR jwt.role = 'superadmin'`. هذا نمط قياسي معروف، لكنه يستحق اختباراً دقيقاً — وهو **السبب الوحيد لجعل جدولي `app_users`/`projects` آخر ما يُفعَّل عليه RLS** (انظر الترتيب في القسم 4).

---

## 2. حجم إعادة البناء من الصفر (الخيار ب)

### 2-أ. ما يُنقل حرفياً مقابل ما يُعاد كتابته

قِستُ قابلية النقل عبر فحص كل ملف: هل يعتمد على React/الواجهة، أم منطق خالص؟

**طبقة المنطق الأساسي — 12 ملف، 1,124 سطر — تُنقل بلا تعديل تقريباً:**

| الملف | أسطر | استدعاءات DB | قابلية النقل |
|---|---:|:---:|---|
| `claude.js` | 491 | 0 | ✅ حرفي |
| `bankReconciliation.js` | 166 | 0 | ✅ حرفي |
| `financialEngine.js` | 145 | 3 (استعلام واحد + دوال نقية) | ✅ شبه حرفي |
| `supplierMatching.js` | 70 | 0 | ✅ حرفي |
| `imageCompress.js` | 55 | 0 | ✅ حرفي |
| `projectSettings.js` | 54 | 0 | ✅ حرفي |
| `journalNumber.js` | 25 | 0 | ✅ حرفي |
| `appReconciliation.js` | 25 | 0 | ✅ حرفي |
| `storage.js` | 31 | 2 | ✅ شبه حرفي |
| `payableBalances.js` | 8 | 0 | ✅ حرفي |
| `appLogo.js` | 48 | 3 | ✅ شبه حرفي |

**10 من 12 ملف خالية تماماً من أي استعلام قاعدة بيانات.** هذه هي "الحقيقة المزعجة" للخيار ب: **المنطق الناضج غير مربوط بالبنية أصلاً، فلا البناء الجديد يحسّنه ولا التأمين الحالي يمسّه.** إنه محايد تماماً بين الخيارين.

**طبقة الواجهة — 20 صفحة، 10,173 سطر — تُعاد كتابتها أو نقلها كما هي:**

هنا الكلفة الحقيقية للخيار ب. الصفحات مربوطة بـReact + Supabase + منطق الأعمال معاً (كما وثّق تقرير review-1). في بناء جديد "نظيف"، إما:
- تُنقل كما هي → لم تكسب "النظافة" التي بُني من أجلها النظام الجديد.
- تُعاد كتابتها → **10,173 سطراً من إعادة الكتابة، وكل سطر فرصة لإعادة خطأ.**

### 2-ب. النضج المحاسبي المهدَّد بإعادة الاختراع

هذا أخطر بند في التقرير كله. قِستُ التاريخ:

```
أول commit: 2026-06-04    آخر commit: 2026-08-11    (~10 أسابيع)
إجمالي: 115 commit    منها 48 إصلاح أخطاء (42%)
```

**48 إصلاحاً متراكماً**، كثير منها أخطاء مالية دقيقة "صامتة" — تعطي رقماً خاطئاً بلا تعطّل ظاهر. نماذج حقيقية من التاريخ:

| الإصلاح | نوع الخطأ | لماذا يُعاد اكتشافه في بناء جديد |
|---|---|---|
| `da96dd2` توحيد `toISOString()` عبر 11 ملف | انزياح يوم كامل بتوقيت +3 | كل مطوّر يكتب `toISOString()` بحسن نية — أُصلح عبر 4 جولات |
| `54e69cc` منع اعتماد تحويل بلا مصدر دفع | سقوط صامت على "عهدة" | منطق `_approveOne` الدقيق — 5 فروع محاسبية |
| `52b4ebe` "غير مصنّف" في تقرير المصروفات | قراءة `document_items` مرتين | تفاعل خفي بين الجداول |
| `8ff2004` عدم تطابق `category_main/sub` للفواتير متعددة البنود | تصنيف خاطئ | منطق `resolveInvoiceType` |
| `deaf86f` إشارة السالب في بطاقات الأرصدة | عرض خاطئ للأرصدة السالبة | — |
| `2322a1e` تنظيف رد Claude قبل JSON.parse | فشل تحليل الفواتير | معالجة control characters + فواصل زائدة في `extractJSON` |

**المنطق الحرج الأكثر عرضة:** `_approveOne` ([PendingDocuments.jsx:309](src/pages/PendingDocuments.jsx#L309)) — دالة واحدة، 5 فروع محاسبية (مبيعات/تحويل/آجل/مصروف ببنود/مصروف بسيط)، وهي المكان الذي يُبنى فيه قيد `ledger_entries` الفعلي. المشروع يبني قيد الدفتر يدوياً في **19 موضعاً** منفصلاً. كل موضع تراكمت فيه إصلاحات دقيقة. إعادة بنائها من الصفر = إعادة المرور بنفس 48 خطأً، **على بيانات مزاهر الحيّة هذه المرة**.

**الحكم على الخيار ب:** يهدم 10,173 سطر واجهة ناضجة (فيها 48 إصلاحاً مدفوناً) لبناء ما تحتاجه فعلاً — 15 سياسة RLS — والذي يمكن إضافته للحالي دون هدم أي شيء.

---

## 3. المخاطرة على مزاهر الحيّة

### 3-أ. الخيار أ (تأمين الحالي) — المخاطرة والتراجع

**احتمال التعطيل: منخفض ومُتحكَّم به، بشرط الترتيب الصحيح.**

الخطر الحقيقي في RLS: لحظة `ENABLE ROW LEVEL SECURITY` على جدول، **كل استعلام بلا سياسة مطابقة يعيد صفر صفوف فوراً** — لا خطأ، بل بيانات فارغة. لو فُعِّل على `ledger_entries` قبل ضبط الجلسة، يرى المحاسب دفتراً فارغاً.

**لكن التراجع فوري وكامل عند كل خطوة:**
```sql
ALTER TABLE x DISABLE ROW LEVEL SECURITY;   -- يعيد كل شيء لحاله في ثانية
```
لا فقدان بيانات، لا هجرة، لا نقطة لا رجعة فيها. **كل خطوة قابلة للاختبار والتراجع مستقلةً.**

**عامل تخفيف حاسم:** مزاهر الآن **المشروع الوحيد** (حذفنا البقية في الجولة السابقة). هذا يعني:
- سياسة RLS تُختبر على مشروع واحد معروف — لا تعقيد متعدد المستأجرين أثناء الانتقال.
- أي خلل يظهر فوراً وواضحاً (دفتر مزاهر إما يعمل أو لا).

### 3-ب. الخيار ب (البناء الجديد) — مخاطر لحظة الترحيل

مزاهر لا تتأثر **أثناء** البناء (قاعدة منفصلة). لكن لحظة الترحيل النهائي تحمل مخاطر **غير قابلة للتراجع**:

| المخاطرة | الوصف |
|---|---|
| فجوة البيانات الحيّة | مزاهر عمل يومي — أي بيانات تُدخَل أثناء الترحيل تضيع أو تُزدوج |
| اختلاف المخطط الدقيق | 15 جدولاً بأعمدة دقيقة (`receivable_in`, `payable_out`, `journal_number`…) — أي انزياح يفسد الأرصدة |
| النضج غير المكتوب | 48 إصلاحاً، بعضها سلوك ضمني غير موثّق — يظهر غيابه بعد الترحيل بأرقام خاطئة |
| نقطة اللا-رجعة | بعد التبديل والعمل على الجديد، العودة للقديم تفقد العمل الجديد |

الخيار ب يؤجّل كل المخاطرة إلى **لحظة واحدة حرجة غير قابلة للتراجع** على بيانات حقيقية — بينما الخيار أ يوزّعها على **15 خطوة صغيرة، كل منها قابل للتراجع فوراً.**

---

## 4. التوصية القاطعة والترتيب الآمن

### التوصية: **تأمين النظام الحالي (الخيار أ). قاطعة، مبنية على الأدلة أعلاه حصراً.**

الأدلة الثلاثة الحاسمة:
1. **الثغرة والنضج في طبقتين منفصلتين.** الثغرة = غياب RLS + مصادقة (طبقة رقيقة، معزولة في `AuthContext.jsx` + المخطط). النضج = 48 إصلاحاً في المنطق المالي (طبقة أخرى، 10 من 12 ملف بلا أي علاقة بقاعدة البيانات). **إصلاح الأولى لا يتطلب لمس الثانية.**
2. **البنية متوافقة مع RLS أصلاً** (كل جدول حول `project_id`، الهوية مركزية). التأمين = **إضافة** 15 سياسة، لا إعادة هيكلة.
3. **المخاطرة قابلة للتوزيع والتراجع** في الخيار أ (15 خطوة، كل `DISABLE` يتراجع فوراً)، بينما الخيار ب يكدّسها في لحظة ترحيل واحدة غير قابلة للتراجع على بيانات حيّة.

البناء من الصفر يُبرَّر حين يكون **الجوهر** فاسداً. هنا الجوهر (المنطق المالي) هو الأنضج والأنظف؛ الفاسد هو **الغلاف الأمني** فقط — وهو الأرخص إصلاحاً والأخطر إعادةَ بناء.

### الترتيب الآمن لتفعيل RLS — جدولاً جدولاً

**المبدأ:** فعّل المصادقة أولاً وتأكد أن الجلسة تحمل `project_id` و`role` في JWT، ثم فعّل RLS من الجداول **الأقل خطورة على العمل اليومي** صعوداً، مع اختبار مزاهر حيّاً بعد كل خطوة.

**المرحلة 0 — تحضير (بلا تفعيل RLS بعد):**
1. أنشئ دالة خادمية `api/session` تتحقق من PIN وتُصدر جلسة Supabase تحمل `project_id`+`role` كـcustom claims.
2. عدّل `AuthContext.jsx` ليستخدمها — الهوية الآن من JWT لا sessionStorage.
3. **اختبار:** مزاهر تسجّل دخول وتعمل بالضبط كما قبل. لا RLS بعد — صفر مخاطرة.

**المرحلة 1 — الجداول الطرفية (أقل استخداماً يومياً، أسهل تراجعاً):**
| # | الجدول | لماذا مبكّراً |
|---|---|---|
| 1 | `loans` · `branches` · `supplier_transactions` · `payable_suppliers` · `suppliers` | فارغة أو شبه فارغة لمزاهر — تفعيل بلا أثر يومي، اختبار نقي للقالب |
| 2 | `quick_sales_draft` | ميزة واحدة (الكاشير) — سهل عزل اختبارها |
| 3 | `attachments` · `categories` · `project_settings` | قراءة غالباً، خطر منخفض |

**المرحلة 2 — الجداول الأساسية (العمل اليومي — بعد أن أثبت القالب نجاحه):**
| # | الجدول | حذر خاص |
|---|---|---|
| 4 | `sales` · `document_items` | تابعة، تُختبر مع الدفتر |
| 5 | `documents` | رفع الفواتير — اختبر الرفع + التحليل بعده |
| 6 | `ledger_entries` | **الأخطر — قلب النظام.** اختبر: الدفتر، التقارير، الأرصدة، الاعتماد، الإدخال السريع |

**المرحلة 3 — جداول الهوية (الأكثر حساسية، آخراً):**
| # | الجدول | لماذا أخيراً |
|---|---|---|
| 7 | `projects` | سياسة superadmin العابرة — تُختبر مع لوحة الإدارة |
| 8 | `app_users` | **يحمي الـPIN — الأهم أمنياً.** سياسة مركّبة (المستخدم يقرأ صفّه، المحاسب يقرأ مشروعه، superadmin يقرأ الكل). فعّله أخيراً لأن خطأً فيه قد يقفل تسجيل الدخول نفسه |

**قاعدة ذهبية عند كل خطوة:** بعد `ENABLE` على أي جدول، افتح مزاهر فوراً واختبر الصفحات التي تلمسه. إن ظهر نقص بيانات → `DISABLE` فوري (ثانية واحدة)، شخّص السياسة، أعد المحاولة. **لا تنتقل للجدول التالي قبل التأكد.**

> **ملاحظة على `app_users` والـPIN:** حتى بعد RLS، الـPIN مخزّن نصاً صريحاً. الإصلاح الكامل (المرحلة اللاحقة) هو تجزئته (hash) وعدم قراءته للواجهة إطلاقاً — التحقق يصير في الخادم فقط. RLS يمنع القراءة العامة؛ التجزئة تمنع كشفه حتى عند خرق الجلسة. الاثنان مكمّلان.

---

## ما لم أفحصه (حدود هذا التقرير)

- لم أفحص إن كانت custom JWT claims تتطلب Supabase Pro أو تعمل على الخطة الحالية — يستحق تأكيداً قبل المرحلة 0.
- لم أقِس زمن تنفيذ المرحلة 0 فعلياً (دالة الجلسة) — يعتمد على طريقة ربط PIN المختارة.
- تقدير "10 أسابيع نضج" مبني على تواريخ الـcommit، وهو حدّ أدنى (لا يشمل عملاً سابقاً للمستودع الحالي).

**لم يُعدَّل أي كود أو قاعدة بيانات أثناء هذه المراجعة.**
