# جولة مراجعة 2 من 5 — الأمان وعزل البيانات

> **تشخيص فقط — لم يُعدَّل أي سطر كود ولا أي صف بيانات.**
> الفحوص التي أُجريت **قراءة فقط**؛ لم أنفّذ أي محاولة كتابة/تعديل/حذف على الإنتاج.
> حالة المستودع: commit `ef643fc`.

---

## ⚠️ الخلاصة التنفيذية

**قاعدة البيانات كلها مكشوفة للقراءة العلنية بلا أي مصادقة.**

فحصت هذا عملياً: باستخدام المفتاح العام المضمّن بحزمة JavaScript (متاح لأي زائر عبر أدوات المطوّر)، **وبلا تسجيل دخول إطلاقاً**، استطعت قراءة:

| ما قُرئ | الكمية |
|---|---|
| جداول مقروءة بالكامل | **15 من 15** |
| إجمالي الصفوف المكشوفة | **3,426 صفاً** |
| قيود دفتر ديوانية مزاهر | **398 قيداً** |
| حسابات المستخدمين (بأرقام الدخول نصاً صريحاً) | **27 حساباً** |
| مستندات/فواتير قابلة للتنزيل | **611 مستنداً** |

بما أن **رقم الدخول (PIN) هو وسيلة المصادقة الوحيدة** بالنظام، وأنه مقروء نصاً صريحاً من جدول `app_users` المكشوف — فإن **كل حساب بالنظام مخترق فعلياً بالتصميم الحالي، بما فيه حساب مزوّد الخدمة (superadmin)**.

### تقييم الخطورة على "ديوانية مزاهر" تحديداً

| # | الثغرة | القراءة | التعديل/الحذف | الخطورة على مزاهر |
|---|---|:---:|:---:|:---:|
| 1 | RLS معطّل كلياً — لا سياسات إطلاقاً | ✅ مؤكَّد عملياً | ⚠️ غير مُختبَر (انظر أدناه) | 🔴 **حرجة** |
| 2 | أرقام الدخول نصاً صريحاً بجدول مكشوف | ✅ مؤكَّد | — | 🔴 **حرجة** |
| 3 | الجلسة والدور بـ`sessionStorage` بلا توقيع | — | — | 🔴 **حرجة** |
| 4 | `api/analyze.js` وسيط مفتوح بلا مصادقة | ✅ مؤكَّد بالكود | — | 🟠 عالية (مالية) |
| 5 | Storage عام + قابل للتعداد | ✅ مؤكَّد عملياً | — | 🔴 **حرجة** |

**ملاحظة منهجية مهمة:** لم أختبر صلاحيات **الكتابة/الحذف** لأنك طلبت صراحة "لا تعدّل أي شيء إطلاقاً"، وأي اختبار كتابة — حتى بشرط `WHERE` يستحيل تطابقه — يظل عملية كتابة على الإنتاج. لذلك خانة "التعديل/الحذف" أعلاه غير محسومة تجريبياً. **لكن الاستنتاج المنطقي قوي جداً**: انعدام سياسات RLS يعني أن دور `anon` يرث صلاحيات الجدول الافتراضية، والقراءة المفتوحة دليل على غياب أي سياسة. للتأكيد القاطع، نفّذ بنفسك بمحرر SQL بـSupabase:

```sql
SELECT relname AS "الجدول", relrowsecurity AS "RLS مفعّل"
FROM pg_class WHERE relnamespace = 'public'::regnamespace AND relkind = 'r'
ORDER BY 1;

SELECT tablename, policyname, cmd FROM pg_policies WHERE schemaname = 'public';
```
لو أعاد الاستعلام الثاني **صفر صفوف** (وهو المتوقع)، فلا توجد أي سياسة، والكتابة مفتوحة كالقراءة.

---

## 1. عزل المشاريع (`project_id`)

### 1-أ. الاستعلامات داخل `src/` — سليمة إجمالاً ✅

راجعت كل استعلامات `src/`. **التصفية بـ`project_id` مطبَّقة بشكل صحيح** بالغالبية الساحقة. الحالات التي بلا فلتر مشروع مبرَّرة:

| الملف : السطر | الاستعلام | الحكم |
|---|---|---|
| [PendingDocuments.jsx:100](src/pages/PendingDocuments.jsx#L100) | `from('projects').select('id,name')` | ✅ مقصود — `isSuperAdmin` فقط |
| [PendingDocuments.jsx:182](src/pages/PendingDocuments.jsx#L182) | `documents` — `if (pid) q = q.eq(...)` | ✅ مشروط بـsuperadmin |
| [SuperAdmin.jsx:414-415](src/pages/SuperAdmin.jsx#L414) | `app_users`/`ledger_entries` بلا فلتر | ✅ مقصود — إحصاء عام |
| [Layout.jsx:31](src/components/Layout.jsx#L31) | `documents` — `if (!isSuperAdmin && projectId)` | ✅ مشروط |
| [AuthContext.jsx:54](src/contexts/AuthContext.jsx#L54) | `app_users` بحثاً عن superadmin | ✅ ضروري لتسجيل الدخول |

**استثناءان يستحقان الانتباه** (تحديث/حذف بـ`id` فقط بلا التحقق من انتماء الصف للمشروع):
- [UsersManagement.jsx:74](src/pages/UsersManagement.jsx#L74) — `update({pin}).eq('id', user.id)`
- [UsersManagement.jsx:84](src/pages/UsersManagement.jsx#L84) — `delete().eq('id', user.id)`

عملياً `user.id` يأتي من قائمة مفلترة بالمشروع أصلاً، فالخطر عبر الواجهة معدوم. لكنه **ليس دفاعاً عميقاً**: مع غياب RLS، لا شيء يمنع استدعاءً مباشراً بمعرّف مستخدم من مشروع آخر.

### 1-ب. مسارات الوصول لبيانات مشروع آخر — **ثلاثة مسارات مؤكدة** 🔴

**المسار 1 — تجاوز الواجهة كلياً (الأخطر):**
لا حاجة لتعديل رابط أو استغلال منطق. المفتاح العام مضمّن بحزمة JS، وRLS معطّل ⇒ أي شخص يفتح الموقع يستطيع من وحدة تحكم المتصفح قراءة **كل بيانات كل المشاريع** مباشرة. أثبتّ هذا عملياً: قرأت 398 قيداً لمزاهر و668 لتشورميك بلا أي تسجيل دخول.

**المسار 2 — انتحال الدور عبر `sessionStorage`:**
[AuthContext.jsx:42-47](src/contexts/AuthContext.jsx#L42) يقرأ الدور والمشروع من `sessionStorage` بلا أي توقيع أو تحقق:
```js
const [role, setRole] = useState(() => sessionStorage.getItem('mz_role') || null)
const [projectId, setProjectId] = useState(() => sessionStorage.getItem('mz_pid') || null)
```
⇒ كتابة سطر واحد بوحدة التحكم تمنح صلاحية `superadmin` على أي مشروع فوراً، بواجهة كاملة تعمل. لا يوجد أي تحقق من جهة الخادم — **لا جلسة، ولا JWT، ولا توقيع**. `ProtectedRoute` يفحص هذا المتغيّر نفسه، فيُخدع بالكامل.

**المسار 3 — سرقة رقم الدخول:**
جدول `app_users` مكشوف ويحتوي عمود `pin` نصاً صريحاً. أي شخص يقرأه ويسجّل دخولاً "شرعياً" بأي حساب — بما فيه `superadmin` الذي [يدخل من أي رابط](src/contexts/AuthContext.jsx#L63) متجاوزاً حصر الـsubdomain.

> **قيد الـsubdomain لا يحمي شيئاً:** [AuthContext.jsx:75-93](src/contexts/AuthContext.jsx#L75) يفلتر المستخدمين بمشروع الـsubdomain — لكن هذا حاجز **واجهة فقط**، يُتجاوز بالكامل عبر المسارين 1 و2.

**تفصيل إضافي:** آلية قفل المحاولات بـ[Login.jsx:76-77](src/pages/Login.jsx#L76) (`attemptsRef` + `lockedUntil`) محفوظة **بذاكرة React فقط** — تتصفّر بتحديث الصفحة، ولا تمنع تخمين PIN آلياً عبر استدعاء Supabase مباشرة. مع PIN من **4 أرقام فقط** (الحد الأدنى المسموح بـ[UsersManagement.jsx:45](src/pages/UsersManagement.jsx#L45))، فضاء التخمين 10,000 احتمال — دقائق معدودة آلياً، حتى لو أُصلح كشف الجدول.

---

## 2. حالة RLS الفعلية

### لا يوجد أي إعداد RLS بالمستودع إطلاقاً
بحثت بكل ملفات `.sql`/`.js`/`.jsx`/`.md`: **صفر** تطابق لـ`ENABLE ROW LEVEL SECURITY` أو `CREATE POLICY`. ملفات المخطط ([supabase-suppliers.sql](supabase-suppliers.sql)، [supabase-tshormik.sql](supabase-tshormik.sql)) تُنشئ جداول بلا أي سياسة أو `GRANT`.

### نتيجة الفحص العملي (قراءة، بلا تسجيل دخول)

| الجدول | القراءة بالمفتاح العام | الصفوف |
|---|:---:|---:|
| `app_users` 🔴 | 🔓 مقروء بالكامل | 27 |
| `ledger_entries` 🔴 | 🔓 مقروء بالكامل | 1,618 |
| `documents` 🔴 | 🔓 مقروء بالكامل | 611 |
| `document_items` | 🔓 مقروء بالكامل | 699 |
| `sales` | 🔓 مقروء بالكامل | 342 |
| `categories` | 🔓 مقروء بالكامل | 109 |
| `projects` | 🔓 مقروء بالكامل | 6 |
| `project_settings` | 🔓 مقروء بالكامل | 5 |
| `payable_suppliers` | 🔓 مقروء بالكامل | 2 |
| `quick_sales_draft` | 🔓 مقروء بالكامل | 2 |
| `suppliers` | 🔓 مقروء بالكامل | 1 |
| `loans` | 🔓 مقروء بالكامل | 4 |
| `supplier_transactions` | 🔓 مقروء بالكامل | 0 |
| `app_settings` | 🔓 مقروء بالكامل | — |
| `bank_statements` | 🔓 مقروء بالكامل | — |

**15 من 15 جدولاً مفتوح.** لا استثناء واحد.

`app_users` أخطرها: أعمدته `id, project_id, name, role, pin, created_at, branch` — **`pin` مقروء نصاً صريحاً** (لم أطبع أي قيمة، ولا يجوز).

---

## 3. الصلاحيات (roles)

### 3-أ. تغطية `ProtectedRoute` — كاملة ✅ لكن بلا أثر أمني حقيقي

كل المسارات الـ18 محمية بـ`ProtectedRoute` بأدوار معقولة. لا يوجد مسار مكشوف. ملاحظتان طفيفتان:

| المسار | الأدوار | ملاحظة |
|---|---|---|
| `/cashier` | `cashier, accountant` | يشمل `accountant` — مقصود على الأرجح (المحاسب يرفع مستندات) |
| `/users` | `accountant` فقط | ⚠️ `superadmin` **مستثنى** — تناقض بسيط مع بقية المسارات؛ لا خطر أمني (superadmin له `/admin` بديلاً) |
| `/suppliers`, `/roastery-sales` | `accountant` فقط | نفس الملاحظة |

**لكن الحماية كلها جمالية:** `ProtectedRoute` ([App.jsx:38-45](src/App.jsx#L38)) يفحص `role` القادم من `sessionStorage` القابل للتحرير. لا يوجد أي تحقق خادمي. **الحماية الحقيقية الوحيدة الممكنة هي RLS — وهي معطّلة.**

### 3-ب. العمليات الحساسة عبر استدعاء مباشر — **كلها ممكنة** 🔴

بما أن RLS معطّل ولا يوجد تحقق خادمي، **كل** عملية حساسة قابلة للتنفيذ من أي طرف (بافتراض أن الكتابة مفتوحة كالقراءة — وهو المرجّح بشدة، انظر التحفّظ المنهجي أعلاه):

| العملية | الكود المعني | الدور المفترض |
|---|---|---|
| حذف قيد نهائياً | [Ledger.jsx:133](src/pages/Ledger.jsx#L133)، [JournalLedger.jsx:220](src/pages/JournalLedger.jsx#L220) | محاسب |
| تعديل مبالغ قيد | [Ledger.jsx:113](src/pages/Ledger.jsx#L113) | محاسب |
| اعتماد مستند وتسجيل قيد | [PendingDocuments.jsx:_approveOne](src/pages/PendingDocuments.jsx#L309) | محاسب |
| تغيير رقم دخول أي مستخدم | [UsersManagement.jsx:74](src/pages/UsersManagement.jsx#L74)، [SuperAdmin.jsx:268](src/pages/SuperAdmin.jsx#L268) | محاسب/superadmin |
| حذف مستخدم | [UsersManagement.jsx:84](src/pages/UsersManagement.jsx#L84) | محاسب |
| إنشاء/تعطيل مشروع | [SuperAdmin.jsx:454-466](src/pages/SuperAdmin.jsx#L454) | superadmin |

⚠️ **ملاحظة تشغيلية مهمة:** أزرار "الإلغاء" بالدفتر تنفّذ **حذفاً نهائياً** (`.delete()`) لا إلغاءً منطقياً — لا يوجد أي أثر تدقيقي (audit trail) ولا إمكانية تراجع. مقترناً بالكتابة المفتوحة، يعني أن **محو قيود مزاهر لا يترك أثراً**. (هذا أيضاً سبب أن `status='cancelled'` غير موجود بأي صف — كما وثّقته بجولة الإصلاح السابقة.)

---

## 4. الأسرار والمفاتيح

### 4-أ. لا تسريب لمفتاح Claude بالواجهة ✅

- `VITE_CLAUDE_API_KEY` موجود بـ[.env](.env) لكنه **غير مستخدم بأي ملف داخل `src/`** — تحققت بالبحث الشامل.
- فحصت حزمة الإنتاج `dist/`: **لا يوجد أي مفتاح بنمط `sk-ant-`**. ✅
- `.env` مُستثنى صحيحاً بـ[.gitignore](.gitignore). ✅
- مفتاح Supabase المضمّن هو `role: anon` (فككت الـJWT للتأكد) — **وليس `service_role`**. ✅ هذا صحيح تصميمياً.

> ⚠️ لكن: كون المفتاح `anon` صحيحاً **لا يعني شيئاً بلا RLS**. المفتاح العام آمن *فقط* حين تحرسه السياسات. حالياً هو مفتاح وصول كامل للقراءة.

**تنظيف مقترح:** `VITE_CLAUDE_API_KEY` و`VITE_PIN_*` بـ`.env` تبدو بقايا غير مستخدمة (`VITE_PIN_*` لا مرجع لها بـ`src/`). أي متغيّر ببادئة `VITE_` **يُضمَّن بالحزمة العلنية تلقائياً** لو استُخدم — الأفضل حذفها لتفادي تسريب مستقبلي بالخطأ.

### 4-ب. `api/analyze.js` — **وسيط مفتوح تماماً** 🟠

```js
export default async function handler(req, res) {
  if (req.method !== 'POST') return res.status(405)...     // الفحص الوحيد
  const apiKey = process.env.CLAUDE_API_KEY...
  const upstream = await fetch('https://api.anthropic.com/v1/messages', {
    headers: { 'x-api-key': apiKey, ... },
    body: JSON.stringify(body),        // ← جسم الطلب يُمرَّر حرفياً بلا فحص
  })
```

**لا يوجد إطلاقاً:** فحص مصادقة · فحص `Origin`/`Referer` · تحديد معدل (rate limit) · تقييد `model` أو `max_tokens` · تحقق من هوية المستخدم.

⇒ **أي شخص بالعالم** يستطيع إرسال `POST` إلى `https://www.tahseeb.app/api/analyze` بأي جسم طلب، واستهلاك رصيد Anthropic الخاص بك بلا حد. الجسم يُمرَّر **حرفياً**، فيمكن للمهاجم اختيار أغلى نموذج وأعلى `max_tokens` واستخدام خدمتك كـChatGPT مجاني على حسابك. نفس المشكلة حرفياً بـ[api/chat.js](api/chat.js) (بلا حتى فحص JSON).

> **أساس هذا الاستنتاج:** قراءة الكود المصدري — قاطعة بذاتها (لا يوجد أي شرط مصادقة بالملف). **لم أنفّذ استدعاءً حياً** للتحقق التجريبي (حُجب الأمر تلقائياً لأنه يستهلك رصيداً فعلياً). للتأكيد بنفسك من أي جهاز:
> ```bash
> curl -X POST https://www.tahseeb.app/api/analyze -H "Content-Type: application/json" -d '{"model":"claude-opus-4-5","max_tokens":1,"messages":[{"role":"user","content":"hi"}]}'
> ```
> عودة استجابة تحتوي `usage` تعني أن الوسيط مفتوح.

---

## 5. تخزين الملفات (Supabase Storage)

**فحص عملي — النتيجة: عام بالكامل وقابل للتعداد** 🔴

| الفحص | النتيجة |
|---|---|
| نمط الروابط المخزّنة | **400 من 400** بنمط `/object/public/` |
| روابط موقّتة موقّعة (`/object/sign/`) | **صفر** |
| جلب ملف حقيقي بلا أي ترويسة مصادقة | **HTTP 200** — `image/jpeg`، 3.4 ميجابايت |
| سرد محتويات الـbucket بالمفتاح العام | **HTTP 200 — مسموح** |

الروابط بنمط:
```
https://<project>.supabase.co/storage/v1/object/public/documents/<PROJECT_ID>/<timestamp>-<random>.<ext>
```
([storage.js:5-13](src/lib/storage.js#L5) — يستخدم `getPublicUrl` وbucket عام)

**نعم — أي شخص بالرابط يصل لفاتورة أي عميل، بلا استثناء.** والأسوأ أن الأمر لا يتطلب معرفة الرابط مسبقاً:

1. جدول `documents` مكشوف ⇒ يحوي **كل** روابط الملفات جاهزة (611 مستنداً)
2. سرد الـbucket مسموح ⇒ تعداد كل الملفات حتى بلا قاعدة البيانات

⇒ **تنزيل كل فواتير وكشوف بنك ديوانية مزاهر ممكن آلياً بسكربت من صفحتين.** الجزء العشوائي بالاسم لا يوفّر أي حماية لأن المسارات مسرودة أصلاً.

---

## خطة المعالجة — مرتّبة بالأثر مقابل الجهد

| # | الإجراء | الأثر | الجهد | ملاحظة |
|---|---|:---:|:---:|---|
| **1** | **تفعيل RLS على الجداول الـ15 + سياسات حسب `project_id`** | 🔴 يغلق 1، 2، 5 معاً | كبير | يحتاج مصادقة حقيقية أولاً (البند 2) |
| **2** | **استبدال PIN بـSupabase Auth** (أو على الأقل تجزئة PIN + جلسة موقّعة) | 🔴 أساس كل ما بعده | كبير | بلا هوية موثوقة خادمياً، RLS لا يمكن كتابته أصلاً |
| **3** | **جعل bucket `documents` خاصاً + روابط موقّعة مؤقتة** | 🔴 يغلق البند 5 | متوسط | `createSignedUrl` بدل `getPublicUrl` — تعديل محدود بـ`storage.js` وأماكن العرض |
| **4** | **حماية `api/analyze.js` و`api/chat.js`** | 🟠 يوقف نزيف الرصيد | صغير | فحص `Origin` + رمز مشترك + تقييد `model`/`max_tokens` + rate limit |
| **5** | إلغاء منطقي (`status='cancelled'`) بدل الحذف النهائي | 🟠 أثر تدقيقي | صغير | يعيد معنى `neq('status','cancelled')` المنتشر بالكود |
| **6** | رفع الحد الأدنى للـPIN + قفل محاولات خادمي | 🟡 | صغير | تحسين تدريجي |
| **7** | حذف `VITE_CLAUDE_API_KEY` و`VITE_PIN_*` غير المستخدمة من `.env` | 🟡 وقائي | دقيقة | تفادي تسريب مستقبلي |
| **8** | إضافة فلتر `project_id` لتحديثات `UsersManagement` | 🟢 دفاع عميق | دقيقة | لا خطر حالي عبر الواجهة |

### ⚠️ ترتيب التنفيذ ملزم
البنود 1 و2 **مترابطة ولا تُنفَّذ منفصلة**: سياسات RLS تحتاج هوية موثوقة من الخادم (`auth.uid()`)، وهي غير موجودة حالياً لأن المصادقة كلها عميلية. تفعيل RLS قبل بناء المصادقة **سيعطّل التطبيق بالكامل** (كل الاستعلامات ستُرفض).

**المسار العملي المقترح:** البند 4 أولاً (صغير، مستقل، يوقف نزيفاً مالياً فورياً) ← ثم 3 (مستقل نسبياً، يغلق أخطر تسريب بيانات) ← ثم 2+1 كمشروع واحد مخطّط.

---

## ما لم يُفحص بهذه الجولة

- **صلاحيات الكتابة/الحذف تجريبياً** — امتثالاً لتعليمة "لا تعدّل أي شيء إطلاقاً". استُنتجت منطقياً؛ استعلام `pg_policies` أعلاه يحسمها نهائياً.
- **استدعاء حي لـ`api/analyze`** — يستهلك رصيداً فعلياً؛ الأمر متاح أعلاه لتنفذه بنفسك.
- سجلات الوصول بـSupabase/Vercel — لتحديد ما إذا كان أي استغلال قد **وقع فعلاً**. ⚠️ **أنصح بمراجعتها**: الانكشاف قائم منذ إنشاء المشروع، لا منذ اليوم.
- أمان التبعيات (`npm audit`) — خارج نطاق أسئلة هذه الجولة.

**لم يُعدَّل أي ملف من ملفات المشروع ولا أي صف بيانات أثناء هذه المراجعة.**
