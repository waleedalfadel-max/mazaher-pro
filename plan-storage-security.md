# خطة: إغلاق ثغرة Storage العام (bucket خاص + روابط موقَّعة)

> **تخطيط فقط — لا تنفيذ.** لا SQL، لا تعديل كود، حتى تراجع وتوافق صراحةً.

---

## 1. الفرق بين الوضع الحالي والحل المقترح

### الوضع الحالي — لماذا هو خطر فعلي لا نظري
كل رفع ملف بالمشروع (فواتير، كشوف بنكية، مرفقات) يمر عبر دالة واحدة فقط: [uploadToStorage()](src/lib/storage.js#L3)، التي تستخدم:
```js
supabase.storage.from('documents').getPublicUrl(path)
```
وتنتج رابطاً دائماً بالشكل:
```
https://<ref>.supabase.co/storage/v1/object/public/documents/<project_id>/<timestamp>-<random>.<ext>
```
هذا الرابط الكامل يُخزَّن حرفياً كنص بثلاثة أعمدة: `documents.file_url`، `ledger_entries.file_url`، `attachments.file_url`.

**السبب الجذري:** إعداد **"Public bucket"** مُفعَّل على مستوى الـbucket نفسه بـSupabase Storage — وهذا إعداد **أسبق من تقييم RLS بالكامل**. مسار `/object/public/...` يتجاوز أي سياسة RLS على `storage.objects` تماماً، بما فيها سياسات "الإصلاح الجذري" التي أضفناها بجولة RLS السابقة (السكوب حسب `auth_project_id()`). تلك السياسات اليوم **لا قيمة أمنية لها للقراءة إطلاقاً** — تتحكم فقط بالرفع (INSERT)، لأن كل قراءة تمر من مسار عام لا يستشيرها أصلاً.

**الأثر:** أي شخص يملك الرابط — بالخطأ، بمشاركة طرف ثالث، بفحص شبكة المتصفح (F12)، بلوج خادم، حتى بتخمين النمط (project_id معروف + timestamp تقريبي) — يفتح صورة الفاتورة أو كشف البنك مباشرة، بلا تسجيل دخول، بلا أي أثر يُسجَّل.

### الحل المقترح
1. **bucket `documents` → Public bucket = false** (إعداد Dashboard واحد، انظر §5).
2. بدل استخدام `file_url` المخزَّن مباشرة كرابط، يُطلَب **رابط موقَّع مؤقت** وقت العرض فقط:
```js
supabase.storage.from('documents').createSignedUrl(path, expiresInSeconds)
```
رابط صالح لدقائق معدودة ثم يتوقف تلقائياً — تسرّبه (نسخ/لصق، مشاركة) محدود الأثر زمنياً.

3. **الميزة الإضافية المهمة:** `createSignedUrl()` نفسها **تمر عبر تقييم RLS** على `storage.objects` قبل إصدار أي توقيع. أي أن مستخدم مشروع "أ" لن يستطيع توليد رابط موقَّع لملف مشروع "ب" حتى لو عرف المسار الدقيق حرفياً — سياسات RLS المفعَّلة مسبقاً (`auth_project_id()`) تستعيد قيمتها الأمنية الكاملة بمجرد أن يصبح الـbucket خاصاً. هذا عزل حقيقي بين المشاريع، وليس مجرد "تعمية بالغموض" (security by obscurity) كحال الروابط المؤقتة وحدها.

⚠️ **افتراض يحتاج تأكيدك قبل أي تنفيذ:** أعتمد على أن سياسات SELECT على `storage.objects` من الإصلاح الجذري (وتحديداً تغطيتها لدور superadmin عبر `auth_is_superadmin()`) لا تزال مفعَّلة وصحيحة — لا أملك وصولاً لفحص `pg_policies` مباشرة (كما طوال هذه الجلسة). سأزوّدك باستعلام تحقق قصير بخطوة §5-صفر تُشغّله أنت بمحرر SQL قبل المتابعة.

---

## 2. كل موضع بالكود يستخدم/يعرض file_url — مصنَّف حسب الحاجة الفعلية للتعديل

فحصت `file_url` (وما يكافئها: `fileUrl`، `getPublicUrl`) بكل ملفات `src/` — 10 ملفات فيها استخدام. **النتيجة المهمة: كل مسارات الكتابة (رفع/حفظ) لا تحتاج أي تعديل** — لأن `uploadToStorage()` تبقى كما هي تماماً (تشرح لاحقاً بـ§4 لماذا)، وهي المصدر الوحيد لكل رفع بالمشروع (تحققت: [InvoiceUpload.jsx:103](src/pages/InvoiceUpload.jsx#L103)، [CashierDashboard.jsx:85](src/pages/CashierDashboard.jsx#L85)، [JournalLedger.jsx:53](src/pages/JournalLedger.jsx#L53)، [BankReconciliation.jsx:163،213](src/pages/BankReconciliation.jsx#L163) — كلها تستدعيها ولا شيء غيرها). التعديل الفعلي مطلوب فقط بمواضع **العرض/الفتح** (8 مواضع بـ7 ملفات).

### أ) مواضع تحتاج تعديلاً فعلياً (عرض/فتح الملف)

| الملف : السطر | ماذا يفعل حالياً | التعديل المطلوب |
|---|---|---|
| [PendingDocuments.jsx:198-199](src/pages/PendingDocuments.jsx#L198) | `loadImage()` — يعرض معاينة الصورة inline من `doc.file_url` مباشرة | جلب رابط موقَّع أولاً، ثم تمريره لـ`_imageData` |
| [PendingDocuments.jsx:225-226](src/pages/PendingDocuments.jsx#L225) | `analyze()` — `fetchAsBase64(doc.file_url)` لإعادة تحليل المستند بالذكاء الاصطناعي | نفس الشيء: توقيع الرابط قبل `fetchAsBase64` |
| [InvoiceUpload.jsx:319](src/pages/InvoiceUpload.jsx#L319) | `<a href={doc.file_url} target="_blank">` بقائمة "آخر المستندات المرفوعة" | تحويل لزر `onClick` غير متزامن يجلب رابطاً موقَّعاً ثم يفتحه |
| [CashierDashboard.jsx:246](src/pages/CashierDashboard.jsx#L246) | نفس النمط بالضبط (قائمة مستنداتي) | نفس الحل |
| [JournalLedger.jsx:126](src/pages/JournalLedger.jsx#L126) | `<a href={a.file_url}>` — قائمة مرفقات جدول `attachments` | نفس الحل |
| [JournalLedger.jsx:392-393](src/pages/JournalLedger.jsx#L392) | `<a href={r.file_url}>` — أيقونة "فتح المستند" بصف حركة الدفتر | نفس الحل |
| [JournalArchive.jsx:427-428](src/pages/JournalArchive.jsx#L427) | نفس أيقونة "فتح المستند" بصف الأرشيف | نفس الحل |
| [Journals.jsx:80-81](src/pages/Journals.jsx#L80) | `<a href={r.file_url}>` — رابط "عرض الفاتورة" | نفس الحل |

**ملاحظة بنيوية على كل الأزرار أعلاه:** `<a href={...}>` يحتاج الرابط جاهزاً وقت الرسم (render) — لا يمكنه انتظار طلب شبكة. لذا كل تحويل يعني تغيير العنصر من `<a href>` إلى `<button onClick={async () => { const url = await getSignedUrl(...); window.open(url, '_blank') }}>`. هذا تغيير بنيوي بسيط ومتكرر بنفس الشكل بكل المواضع الثمانية — لا منطق مختلف بينها.

### ب) حالتان خاصتان: روابط مضمَّنة بمستندات مُصدَّرة (PDF / طباعة)

| الملف : السطر | السياق | لماذا "خاصة" |
|---|---|---|
| [Reports.jsx:299-304](src/pages/Reports.jsx#L299) | `exportPdf()` — يبني `linkData` من `docs[i].file_url` ويُدرجها كروابط قابلة للنقر داخل ملف PDF مُصدَّر (`pdf.link(...)`) | ملف PDF محفوظ محلياً قد يُفتح بعد أسابيع — رابط موقَّع بصلاحية دقائق سيكون منتهياً حينها بالتأكيد |
| [JournalArchive.jsx:88](src/pages/JournalArchive.jsx#L88) | قالب HTML للطباعة/التصدير: `<a href="${r.file_url}">📎 مستند</a>` | نفس المشكلة تماماً — رابط مضمَّن بمستند يُطبَع/يُحفَظ خارج التطبيق |

**قرار مطلوب منك (وليس تقنياً بحتاً):**
- **الخيار الموصى به:** حذف الرابط القابل للنقر من هذين التصديرين تماماً، والإبقاء على اسم الملف كنص عادي فقط (بلا `<a>`). المستند يبقى متاحاً بالفتح من داخل التطبيق نفسه (حيث الرابط الموقَّع يُولَّد لحظياً)، لا من نسخة PDF/ورقية قد تُشارَك خارج التطبيق أصلاً — وهذا يتماشى مع الهدف الأمني نفسه (منع تسرّب روابط دائمة عبر قنوات خارج النظام).
- **البديل:** تضمين رابط موقَّع بصلاحية طويلة جداً (أشهر) بدل حذفه — لكن هذا يُضعف الحماية جزئياً لهذا المسار تحديداً (رابط طويل الأمد داخل ملف قد يُشارَك بلا رقابة)، ويُعيد نفس فئة الخطر الأصلي وإن بمدى أضيق.

سأمضي بالخيار الموصى به ما لم تُخبرني غير ذلك.

### جـ) مواضع كتابة فقط — لا تحتاج أي تعديل (للتوثيق، ليست عمل مطلوب)
`PendingDocuments.jsx` (نسخ `doc.file_url` إلى `ledger_entries.file_url` عند الاعتماد، أسطر 334/341/398/447/491) · `Suppliers.jsx:85` · `Ledger.jsx:162` · `lib/bankReconciliation.js:161` · جميع استدعاءات `uploadToStorage()`. كلها تتعامل مع `file_url` كسلسلة نصية تُنسَخ أو تُخزَّن فقط، بلا أي عرض/فتح مباشر.

**اكتشاف جانبي مهم أثناء الفحص:** [lib/appLogo.js](src/lib/appLogo.js) يستخدم **نفس الـbucket** (`documents`) لتخزين شعار التطبيق بمسار ثابت `__app__/logo.png`، ويُعرَض بشاشة الدخول **قبل أي تسجيل دخول** (زائر مجهول تماماً). هذا استخدام عام ومقصود، ويجب أن يبقى كذلك. **يجب استثناؤه من أي حل شامل يعتمد على خصوصية الـbucket بالكامل** — أفصّل الخيارات بـ§5.

---

## 3. الآثار الجانبية المحتملة

| الأثر | التفصيل | التخفيف |
|---|---|---|
| **انتهاء صلاحية الروابط** | كل رابط موقَّع صالح لمدة محدودة (مقترح: 5 دقائق) | يُولَّد وقت الطلب الفعلي (عند الضغط على الزر) لا مسبقاً — المستخدم يفتح الملف خلال ثوانٍ من توليده، فانتهاء الصلاحية لا يُلاحَظ عملياً بالاستخدام العادي |
| **طلب شبكة إضافي لكل فتح** | كل ضغطة "فتح مستند" تصبح: طلب توقيع (سريع جداً، لا يرفع/ينزّل الملف نفسه) ثم فتح الرابط — لا مضاعفة حقيقية بالوقت المُدرَك | لا حاجة لتخفيف — الفرق ببضع مئات المللي ثانية |
| **`<a href>` تصبح `<button onClick>`** | فقدان سلوكيات المتصفح الافتراضية على الروابط: "فتح بتبويب جديد" من قائمة الفأرة اليمنى، نسخ الرابط، معاينة الرابط بشريط الحالة عند التحويم | مقبول — الوظيفة الأساسية (فتح الملف بتبويب جديد) تبقى عبر `window.open(url,'_blank')`؛ الفقدان هامشي |
| **مراجعة مستند طويلة (أكثر من صلاحية الرابط)** | مثلاً محاسب يترك معاينة صورة مفتوحة (`PendingDocuments.jsx`) لأكثر من 5 دقائق دون تفاعل | إن استمر التحميل من `_imageData` (Base64 مُخزَّن بالحالة محلياً بعد أول تحميل) فلا تأثير — الصورة محمَّلة أصلاً بالمتصفح؛ يحتاج تحققاً عملياً بخطوة الاختبار |
| **فقدان الرابط القابل للنقر بتصدير PDF/طباعة** | مذكور بـ§2-ب — قرار مقصود، ليس عطلاً | التطبيق نفسه يبقى مصدر العرض الوحيد للمستند الأصلي |
| **تعقيد كود إضافي طفيف** | دالة مساعدة واحدة جديدة (`getSignedUrl`) + تحويل 8 مواضع من متزامن لغير متزامن | معالجة أخطاء بسيطة (فشل توليد التوقيع → رسالة "تعذّر فتح الملف" بدل انهيار الصفحة) |
| **صلاحية superadmin عبر المشاريع** | إن لم تكن سياسة RLS على `storage.objects` تغطي `auth_is_superadmin()` للقراءة، سيفقد superadmin القدرة على فتح ملفات أي مشروع بعد التحويل للخاص | **يجب التحقق من هذا صراحة قبل التنفيذ** (استعلام SQL بـ§5-صفر) — لو غير مغطاة، يلزم إضافتها ضمن نفس دفعة SQL |

---

## 4. هل يكسر تحويل الـbucket المستندات الحالية (277+)؟

**لا — الملفات نفسها لا تُمَس إطلاقاً، ولا حاجة لأي ترحيل بيانات.**

السبب: `getPublicUrl(path)` بـSupabase هي **دالة بناء نص محلية بحتة** (لا تستدعي الشبكة، لا تتحقق من وجود الملف أو حالة الـbucket) — تُنتج ببساطة:
`<SUPABASE_URL>/storage/v1/object/public/<bucket>/<path>`.

بما أن **كل** الروابط المخزَّنة اليوم (بالأعمدة الثلاثة) أُنتِجت حصراً عبر هذه الدالة نفسها (لا مصدر آخر — تحققت من كل استدعاء)، فكلها تتبع نفس النمط الثابت بدقة. هذا يعني:
- المسار الحقيقي داخل الـbucket (`<project_id>/<timestamp>-<random>.<ext>`) **قابل للاستخلاص حرفياً** من أي `file_url` قديم، بعملية نصية بسيطة (حذف البادئة الثابتة `.../object/public/documents/`).
- دالة `getSignedUrl(storedUrl)` مساعدة واحدة تعمل بنفس الكفاءة على مستند رُفع اليوم أو قبل سنة — **لا فرق بين قديم وجديد إطلاقاً**، ولا صلة بعدد المستندات (277 أو 27,000 سواء).
- **لا تُعدَّل قاعدة البيانات، لا تُنقَل الملفات بالـStorage، لا حاجة لأي سكربت ترحيل.** التغيير الوحيد الفعلي هو: (أ) إعداد "Public" بمستوى الـbucket، (ب) طريقة توليد الرابط وقت العرض بالكود.

النتيجة العملية بعد تفعيل الخصوصية: الرابط القديم `.../object/public/...` المخزَّن يتحوّل من "رابط يعمل مباشرة" إلى "قيمة تُستخدَم فقط كمُدخَل لتوليد رابط موقَّع" — يبقى صالحاً كمعرِّف مسار للأبد، ويتوقف عن العمل كرابط مباشر بمجرد التبديل.

---

## 5. خطة تنفيذ تدريجية آمنة — مع تراجع فوري

### الخطوة صفر — تحقق قبل أي شيء (أنت، بمحرر SQL)
استعلام تأكيد أن RLS على `storage.objects` تغطي القراءة لـ`authenticated` (سكوب مشروع) و`superadmin` (بلا سكوب). أزوّدك بالاستعلام الدقيق عند موافقتك على الخطة — لا أُنفّذه بنفسي (لا أملك وصول SQL كما تعلم).

### الخطوة 1 — إضافة الدالة المساعدة (كود فقط، بلا أي تغيير بالسلوك الحالي)
دالة `getSignedUrl(storedUrl, expiresIn = 300)` بـ`lib/storage.js` — تستخلص المسار من الرابط المخزَّن وتُنادي `createSignedUrl`. **إضافة صرفة، غير مستدعاة من أي مكان بعد — صفر خطورة.**

### الخطوة 2 — تحويل الـ8 مواضع للعرض (كود فقط، الـbucket لا يزال عاماً)
تعديل كل موضع بجدول §2-أ + قرار §2-ب لاستخدام `getSignedUrl()`. **لأن الـbucket لا يزال Public بهذه المرحلة**، فكل من: الرابط المباشر القديم (لو بقي بمكان لم يُعدَّل بالخطأ) **و** الرابط الموقَّع الجديد يعملان معاً بلا تعارض — هامش أمان مزدوج أثناء التطوير والاختبار.

**بناء + اختبار كامل بهذه المرحلة (§6 بالأسفل) قبل الانتقال للخطوة التالية — هذا هو الاختبار الحقيقي للكود الجديد، بلا أي مخاطرة على الوصول الحالي.**

### الخطوة 3 — نشر الكود (commit + push + vercel --prod)
كالمعتاد. الوضع بعدها: كل الأزرار تستخدم روابط موقَّعة فعلياً بالإنتاج، لكن الـbucket لا يزال عاماً — **لا تحسّن أمني فعلي بعد**، فقط تأكيد أن الكود الجديد يعمل صحيحاً بالإنتاج الحقيقي قبل أي قطع للوصول القديم.

### الخطوة 4 — استثناء شعار التطبيق (قرار تصميم صغير قبل الخطوة الحرجة)
بما أن `__app__/logo.png` يُعرَض قبل تسجيل الدخول (§2-جـ)، خياران:
- **(موصى به) نقل الشعار لـbucket عام منفصل** (مثلاً `public-assets`) — فصل نظيف بين "أصول عامة مقصودة" و"مستندات مالية خاصة"، بلا أي تعقيد إضافي بمنطق التوقيع.
- أو: سياسة RLS استثنائية بـ`storage.objects` تسمح بقراءة عامة لمسار `__app__/*` تحديداً حتى مع الـbucket خاص (Supabase تدعم هذا: bucket خاص + policy عامة على مسار محدد) — أكثر تعقيداً، بلا داعٍ حقيقي.
سأمضي بالخيار الموصى به (bucket منفصل) ما لم تُفضّل غيره — نقل ملف شعار واحد، تحديث سطرين بـ`appLogo.js` (`BUCKET` الثابت)، صفر أثر على المستندات المالية.

### الخطوة 5 — التبديل الفعلي (اللحظة الحرجة)
Supabase Dashboard → Storage → `documents` → إيقاف "Public bucket". إجراء فوري، عكسي بالكامل بنقرة واحدة.

### الخطوة 6 — تحقق فوري بعد التبديل
إعادة تشغيل كل اختبارات §6 مباشرة على الإنتاج الحي — يجب أن تعمل بلا أي فرق ملحوظ عن الخطوة 3 (لأن الكود لم يتغيّر، فقط توقف المسار القديم عن العمل).

### خطة التراجع
في أي لحظة بعد الخطوة 5: إعادة تفعيل "Public bucket" فوراً (نفس الإعداد، عكس الاتجاه) — يستعيد الوضع السابق تماماً بلا فقدان بيانات، لأن لا شيء نُقِل أو حُذِف بأي خطوة. لو الخلل بالكود نفسه (خطوة 2-3) بدل إعداد الـbucket، فالرجوع أسهل: revert commit عادي، الـbucket أصلاً لم يتغيّر بعد بتلك المرحلة.

---

## 6. خطة اختبار شاملة

### تغطية الأدوار × الصفحات
| الدور | الصفحات/الإجراءات |
|---|---|
| **accountant** | فتح مستند من `PendingDocuments` (معاينة + إعادة تحليل)، فتح مرفق من `JournalLedger`، فتح مستند من صف بالدفتر (`JournalLedger`/`JournalArchive`)، رفع كشف بنكي بـ`BankReconciliation` ثم فتحه، تصدير PDF من `Reports` والتحقق من المحتوى (بلا روابط مكسورة أو placeholder فارغ) |
| **owner** | فتح مستند من `Journals`، تصدير/معاينة PDF من `Reports` |
| **purchasing** | رفع فاتورة، فتح من "آخر المستندات المرفوعة" بـ`InvoiceUpload` |
| **cashier** | رفع مستند، فتح من "آخر المستندات المرفوعة" بـ`CashierDashboard` |
| **superadmin** | فتح مستند من مشروع (أي مشروع) وهو بوضع "إدارة العملاء" — تأكيد عدم فقدان الصلاحية عبر المشاريع (النقطة الحرجة من §3) |

### اختبارات سلبية (الأهم — تثبت أن الثغرة أُغلقت فعلاً)
1. نسخ `file_url` قديم مباشرة (من أي صف بقاعدة البيانات قبل التعديل) ولصقه بمتصفح خاص (Incognito، بلا أي جلسة) بعد الخطوة 5 — **يجب أن يفشل** (403/400) بدل عرض الملف. هذا هو الدليل الحاسم على إغلاق الثغرة.
2. محاولة فتح رابط موقَّع بعد انتهاء صلاحيته يدوياً (تعيين `expiresIn` قصير جداً مؤقتاً بالاختبار، أو الانتظار) — يجب أن يفشل بوضوح، والضغط على "فتح" مجدداً يُولّد رابطاً جديداً صالحاً.
3. (إن أمكن تحضير بيانات اختبار بمشروع افتراضي ثانٍ) تأكيد أن مستخدم مشروع "أ" لا يستطيع توليد رابط موقَّع لملف مشروع "ب" حتى بمعرفة المسار الدقيق — يثبت العزل الحقيقي بين المشاريع، لا فقط انتهاء الصلاحية الزمني.

### اختبار الانحدار (Regression)
- شعار التطبيق يظهر بشاشة الدخول (زائر مجهول، بلا جلسة) بعد نقله للـbucket المنفصل.
- `npm run build` ينجح بعد كل خطوة.
- لا تراجع بوظيفة رفع أي مستند (لم تتغيّر مسارات الرفع إطلاقاً حسب §4).

---

## نقطتا قرار بحاجة موافقتك الصريحة قبل التنفيذ
1. **§2-ب** — حذف الروابط القابلة للنقر من تصدير PDF (`Reports.jsx`) وقالب الطباعة (`JournalArchive.jsx`) بدل تضمين رابط طويل الأمد؟ (موصى به)
2. **§5-خطوة4** — نقل شعار التطبيق لـbucket عام منفصل بدل استثناء بسياسة RLS؟ (موصى به)

بانتظار مراجعتك — لا تنفيذ حتى موافقتك الصريحة.
