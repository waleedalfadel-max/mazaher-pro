-- ═══════════════════════════════════════════════════════════════════════════
-- المرحلة 1 من التأمين — جسر مصادقة المالك (Supabase Auth) بجانب PIN
-- شغّل هذا في: لوحة Supabase → SQL Editor
-- آمن تماماً: عمود واحد اختياري (nullable). لا يمسّ أي صف قائم. لا RLS هنا.
-- ═══════════════════════════════════════════════════════════════════════════

-- الخطوة 1: أضف عمود الربط بهوية Supabase Auth
ALTER TABLE app_users ADD COLUMN IF NOT EXISTS auth_id UUID UNIQUE;

-- تحقق: يجب أن يظهر العمود، وكل الصفوف الحالية auth_id = NULL (تعمل بـPIN كما هي)
-- SELECT name, role, (auth_id IS NULL) AS pin_only FROM app_users ORDER BY role;


-- ═══════════════════════════════════════════════════════════════════════════
-- الخطوة 2 (تُنفَّذ بعد إنشاء هوية المالك من لوحة Authentication):
-- اربط صف المالك بهويته الجديدة. استبدل <AUTH_UID> بالمعرّف الظاهر في
-- Authentication → Users بعد إنشاء المستخدم.
--
-- ⚠️ لا تشغّل هذا السطر قبل إنشاء الهوية ونسخ الـUID الحقيقي.
-- ═══════════════════════════════════════════════════════════════════════════

-- UPDATE app_users
-- SET auth_id = '<AUTH_UID>'
-- WHERE role = 'owner'
--   AND project_id = '03149efa-1def-45ec-91b2-2d2b24e38e8c';   -- ديوانية مزاهر

-- تحقق بعد الربط: يجب أن يعيد صفاً واحداً (المالك)
-- SELECT name, role FROM app_users WHERE auth_id = '<AUTH_UID>';


-- ═══════════════════════════════════════════════════════════════════════════
-- للتراجع (إن لزم — يزيل الجسر بلا أي أثر على PIN):
-- ALTER TABLE app_users DROP COLUMN IF EXISTS auth_id;
-- ═══════════════════════════════════════════════════════════════════════════
