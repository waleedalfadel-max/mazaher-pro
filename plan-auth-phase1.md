# المرحلة 1 من تأمين النظام — مصادقة المالك (Supabase Auth) بجانب PIN

> **الحالة:** معتمدة ونُفِّذت في الكود. خطوات قاعدة البيانات + إنشاء هوية المالك يدوية (بيد المالك — لا تُلمس كلمة المرور من الكود).
> **المبدأ الحاكم:** إضافة خالصة. مسار PIN لم يُمسّ إطلاقاً. كل شيء قابل للتراجع الفوري.

## السياق
النظام يعمل الآن بمصادقة PIN فقط (5 أرقام مقابل `app_users.pin` نصاً صريحاً). لا يوجد أي Supabase Auth. هذه أول خطوة نحو RLS: نمنح **المالك** هوية Supabase حقيقية (بريد + كلمة مرور) تحمل `auth.uid()` — أساس سياسات RLS لاحقاً — مع بقاء الجميع (بمن فيهم المالك احتياطاً) قادرين على الدخول بـPIN حتى تكتمل بقية المراحل.

## القرارات المعتمدة
1. **بجانب PIN لا بدله** — المالك يستطيع الدخول بالطريقتين. بقية الأدوار بـPIN فقط.
2. **عمود `auth_id` على `app_users`** (لا `owner_auth_id` على `projects`) — الأنسب لأن الربط بين هوية Supabase والمستخدم منطقياً على مستوى المستخدم لا المشروع، ويعمّم لاحقاً على كل الأدوار بنفس النمط.
3. **PIN يبقى شبكة أمان** — لا يُحذف ولا يُعطَّل في هذه المرحلة.

## 1. التعايش بين النظامين (كيف لا نكسر PIN)
- الدالة `login(pin)` في [AuthContext.jsx](src/contexts/AuthContext.jsx) **لم يتغيّر منها حرف واحد**.
- أُضيفت `loginWithEmail(email, password)` منفصلة تماماً: تنادي `supabase.auth.signInWithPassword` ثم تبحث عن صف المستخدم بـ`auth_id = session.user.id`، وتضبط **نفس** حالة الجلسة التي يضبطها PIN (`role`, `projectId`, `projectName`, `branch`, `modules` + نفس مفاتيح `sessionStorage`).
- النتيجة: بعد الدخول، كل الصفحات الـ20 تقرأ الهوية عبر `useAuth()` بلا أي علم بطريقة الدخول — لا صفحة تحتاج تعديلاً.
- شاشة الدخول: لوحة PIN كما هي تماماً، وأسفلها رابط صغير "دخول المالك بالبريد الإلكتروني" يكشف حقلي بريد/كلمة مرور. المالك يختار.

## 2. تغييرات قاعدة البيانات (يدوية — [supabase-auth-phase1.sql](supabase-auth-phase1.sql))
عمود واحد إضافي، nullable، لا يمسّ أي صف قائم:
```sql
ALTER TABLE app_users ADD COLUMN IF NOT EXISTS auth_id UUID UNIQUE;
```
- Nullable ⇒ كل صفوف PIN الحالية تبقى `auth_id = NULL` وتعمل كما هي.
- `UNIQUE` ⇒ يمنع ربط هويتين بنفس المستخدم.
- **لا RLS في هذه المرحلة إطلاقاً** — الجداول تبقى مفتوحة كما هي. هذه الخطوة تبني الجسر فقط.

## 3. ربط حساب مالك مزاهر الحالي (بلا فقدان وصول)
صف المالك موجود (`role=owner`, `name=المالك`, `project_id=03149efa…`). لا يُحذف ولا يُنشأ جديد — يُضاف له `auth_id` فقط:
1. المالك يُنشئ هوية Supabase بإيميله + كلمة مروره (لوحة Supabase — الخطوات الدقيقة تُسلّم بعد التنفيذ).
2. يُنسخ `auth.uid()` الناتج ويُوضع في صف المالك: `UPDATE app_users SET auth_id = '<uid>' WHERE role='owner' AND project_id='03149efa…'`.
3. المالك الآن يدخل بالطريقتين. لو فشل البريد لأي سبب → PIN يعمل فوراً (لم يُمسّ).

## 4. ملفات الكود الممسوسة + التراجع
| الملف | التغيير | التراجع |
|---|---|---|
| [AuthContext.jsx](src/contexts/AuthContext.jsx) | + `loginWithEmail`، + `applyIdentity` (تخصّ البريد فقط)، + استعادة جلسة عند التحميل (دفاعية)، + سطر `signOut` في `logout`. `login(pin)` بلا مساس | `git revert` — والعمود يبقى بلا ضرر |
| [Login.jsx](src/pages/Login.jsx) | + وضع بريد خلف رابط. لوحة PIN بلا مساس | `git revert` |
| [supabase-auth-phase1.sql](supabase-auth-phase1.sql) | ملف جديد (لا يُشغَّل تلقائياً) | لا شيء يُنفَّذ بلا تشغيلك اليدوي |

**ضمانات عدم الكسر:**
- استعلام `auth_id` لا يُنفَّذ إطلاقاً إلا عند وجود جلسة Supabase (لا توجد قبل أول دخول بريد ناجح، الذي يتطلب العمود أصلاً) — فلا نافذة يُستعلم فيها عن عمود غير موجود. ومع ذلك مغلّف بـ`try/catch` احتياطاً.
- `logout` يضيف `supabase.auth.signOut()` — عملية لا أثر لها لمستخدم PIN (لا جلسة Supabase لديه).
- لا تغيير في `vercel.json` ولا في مفتاح Supabase ولا في أي صفحة أخرى.

## 5. خطة الاختبار بعد التنفيذ
**قبل الخطوات اليدوية (البريد غير مفعّل بعد):**
1. `npm run build` ✅
2. دخول مزاهر بـPIN لكل الأدوار (مالك/محاسب/كاشير/مشتريات) — يعمل كما قبل تماماً.
3. الدفتر، التقارير، رفع مستند، الإدخال السريع — تعمل.
4. الضغط على "دخول المالك بالبريد" يكشف الحقلين، والرجوع للـPIN يعمل. (محاولة الدخول بالبريد قبل الربط تُظهر رسالة خطأ واضحة، لا تكسر شيئاً.)

**بعد الخطوات اليدوية (ALTER TABLE + إنشاء الهوية + الربط):**
5. المالك يدخل بالبريد + كلمة المرور → يصل للوحة التحكم بنفس صلاحياته.
6. تحديث الصفحة (F5) → يبقى داخلاً (استعادة الجلسة).
7. تسجيل الخروج → يخرج، وجلسة Supabase تُنهى (محاولة F5 لا تعيده).
8. المالك يدخل بـPIN أيضاً (شبكة الأمان) → يعمل.
9. بقية الأدوار — لا تزال PIN فقط، بلا تأثّر.

## ما ليس في هذه المرحلة (مراحل لاحقة)
- تفعيل RLS على أي جدول (المرحلة التالية).
- تجزئة (hash) الـPIN وإخفاؤه عن الواجهة.
- تعميم البريد على بقية الأدوار.
- ربط `auth.uid()` بسياسات RLS.
