# المرحلة 2 — تفعيل RLS

> **تخطيط فقط. لا SQL نُفِّذ، لا كود تغيّر.** كل الأرقام أدناه من قراءة حيّة لقاعدة البيانات والكود بتاريخ اليوم (بعد نجاح المرحلة 1 — عمود `auth_id` موجود، والمالك مربوط).

---

## ⚠️ اكتشاف يغيّر ترتيب التنفيذ — اقرأه قبل أي شيء آخر

طلبتَ ترتيب **13 جدولاً**. الجرد الحي وجد **15 جدولاً** فعلياً تحتاج RLS (نفس الرقم الذي وثّقه `review-3-rebuild-vs-secure.md` سابقاً: 13 جدولاً بنمط `project_id` موحّد + `projects` + `app_users` بمعالجة خاصة). أستخدم الـ15 أدناه — التصحيح موثّق لأنه يغيّر الترتيب.

**والأهم من فرق العدد — عائق تقني يمنع تفعيل RLS الحقيقي على أغلب الجداول الآن:**

فحصت `app_users.auth_id` حياً:

| الدور | `auth_id` |
|---|:---:|
| owner | ✅ مربوط (المرحلة 1) |
| accountant | ❌ NULL |
| purchasing | ❌ NULL |
| cashier | ❌ NULL |
| superadmin | ❌ NULL |

**السبب التقني (هذا ليس افتراضاً — هذا سلوك Postgres/Supabase الموثّق):** حين يستخدم المتصفح `anon key` بلا جلسة `supabase.auth`، تصل كل الطلبات للخادم بدور Postgres `anon`، ودالة `auth.uid()` تُرجع **`NULL`** دائماً لهذا الدور (لا يوجد `sub` claim بمفتاح anon الثابت). فقط بعد `supabase.auth.signInWithPassword` (ما فعلناه للمالك فقط بالمرحلة 1) تتحول الطلبات لدور `authenticated` ويُرجع `auth.uid()` القيمة الحقيقية.

**النتيجة الحتمية:** أي سياسة RLS تعتمد على `auth.uid()` ستُرجع **صفر صفوف** (قراءة) أو **ترفض الكتابة** (إدراج/تعديل/حذف) لأي طلب من المحاسب أو الكاشير أو مسؤول المشتريات — **فوراً وبالكامل** — لأنهم لا يملكون جلسة Supabase حقيقية بعد. هذا ليس احتمالاً بنسبة كذا% — هذه آلية Postgres القطعية. تفعيل RLS بنمط `auth.uid()` اليوم على أي جدول يلمسه هؤلاء الثلاثة = تعطيل فوري لعملهم.

**كم جدولاً يلمسونه؟** كل الجداول الـ15 تقريباً — عدا اثنين لا يلمسهما أي كود حالياً إطلاقاً (تفصيل بالقسم 1).

**⇒ هذا يعيد تشكيل الجدول الزمني المطلوب بالبند 4، ويجعل البند 3 (الذي طلبت التدقيق فيه) هو **الحاجز الفعلي**، لا مجرد ملاحظة جانبية.** التفصيل الكامل بالقسم 3 أدناه، بما فيه التوصية.

---

## 1. ترتيب الجداول الـ15 — الأكثر أماناً إلى الأخطر

المعيار: (أ) هل يلمسه أي كود حالياً؟ (ب) كم استدعاءً بالواجهة؟ (ج) كم دوراً (خصوصاً PIN-only) يعتمد عليه يومياً؟ (د) هل هو جدول هوية/تسجيل دخول له خصوصية إضافية؟

| # | الجدول | صفوف اليوم | استدعاءات الكود | الأدوار اللامسة | الفئة |
|---|---|---:|---:|---|---|
| 1 | `loans` | 0 | **0** | لا أحد — Loans.jsx يقرأ `ledger_entries` فقط | 🟢 آمن مطلق |
| 2 | `branches` | 0 | **0** | لا أحد — قائمة الفروع تُشتق من `ledger_entries.branch` | 🟢 آمن مطلق |
| 3 | `supplier_transactions` | 0 | 3 | accountant فقط | 🟡 محظور مؤقتاً |
| 4 | `suppliers` | 0 | 3 | accountant فقط | 🟡 محظور مؤقتاً |
| 5 | `payable_suppliers` | 0 | 6 | accountant, owner | 🟡 محظور مؤقتاً |
| 6 | `document_items` | 239 | 2 | accountant | 🟡 محظور مؤقتاً |
| 7 | `attachments` | 1 | 4 | accountant | 🟡 محظور مؤقتاً |
| 8 | `quick_sales_draft` | 2 | 5 | **cashier فقط** (نطاق ضيق جداً رغم الاستخدام اليومي) | 🟡 محظور مؤقتاً |
| 9 | `categories` | 23 | 7 | purchasing, cashier, accountant | 🟡 محظور مؤقتاً |
| 10 | `sales` | 72 | 8 | owner, accountant, cashier | 🟡 محظور مؤقتاً |
| 11 | `project_settings` | 1 | 8 | كل الأدوار (عبر `lib/projectSettings.js`) | 🟡 محظور مؤقتاً |
| 12 | `documents` | 276 | 28 | **كل الأدوار غير superadmin** (أوسع نطاق) | 🟡 محظور مؤقتاً |
| 13 | `ledger_entries` | 404 | **43** | owner, accountant, cashier — **قلب النظام** | 🔴 الأخطر بين "العاديين" |
| 14 | `projects` | 1 | 8 | **كل محاولة دخول PIN** (حتى قبل أي جلسة) | ⚫ خاص — يحتاج تصميماً مختلفاً |
| 15 | `app_users` | 5 | 16 | **كل محاولة دخول PIN** + يحوي أرقام PIN نصاً صريحاً | ⚫ الأخطر إطلاقاً |

**لماذا `projects` و`app_users` بفئة منفصلة (⚫) لا مجرد "آخر الترتيب":**
تسجيل الدخول بـPIN نفسه يستعلم عن هذين الجدولين **بدور anon قبل وجود أي جلسة على الإطلاق** ([AuthContext.jsx:54-93](src/contexts/AuthContext.jsx#L54)): يبحث عن `projects.id` بالـsubdomain، ثم عن `app_users` بمطابقة `pin` نصياً. سياسة RLS تعتمد على `auth.uid()` هنا لن "تُبطئ" الدخول — **ستمنعه نهائياً لكل الأدوار من اليوم الأول**، بما فيها محاولة المالك تسجيل الدخول بـPIN كشبكة الأمان التي أكّدنا عملها بالمرحلة 1. هذان الجدولان يحتاجان تصميم سياسة مختلفاً جذرياً (القسم 3-جـ)، وليس فقط "تأجيلاً" كبقية الجداول.

---

## 2. السياسات — SQL جاهز لكل جدول

### 2-0. الأساس المشترك (يُنشأ مرة واحدة، تستخدمه كل سياسات project_id)

بدل تكرار الاستعلام الفرعي 13 مرة (نفس مبدأ تفادي التكرار الذي رصدناه بمراجعة المعمارية):

```sql
CREATE OR REPLACE FUNCTION auth_project_id() RETURNS UUID
LANGUAGE sql SECURITY DEFINER STABLE AS $$
  SELECT project_id FROM app_users WHERE auth_id = auth.uid() LIMIT 1
$$;

CREATE OR REPLACE FUNCTION auth_is_superadmin() RETURNS BOOLEAN
LANGUAGE sql SECURITY DEFINER STABLE AS $$
  SELECT EXISTS (SELECT 1 FROM app_users WHERE auth_id = auth.uid() AND role = 'superadmin')
$$;
```
`SECURITY DEFINER` ضروري: تسمح للدالة بقراءة `app_users` حتى لو صار عليه RLS لاحقاً (الفئة ⚫)، بلا تعليق دائري.

**تحقق:** `SELECT auth_project_id(), auth_is_superadmin();` — بجلسة anon تُرجع `NULL, false` (متوقع ومصمَّم).
**تراجع:** `DROP FUNCTION IF EXISTS auth_project_id(); DROP FUNCTION IF EXISTS auth_is_superadmin();`

### 2-1/2-2. `loans` و`branches` — 🟢 يمكن تفعيلها اليوم فوراً (Tier 0)

```sql
ALTER TABLE loans ENABLE ROW LEVEL SECURITY;
CREATE POLICY loans_project_isolation ON loans FOR ALL
  USING (project_id = auth_project_id() OR auth_is_superadmin())
  WITH CHECK (project_id = auth_project_id() OR auth_is_superadmin());

ALTER TABLE branches ENABLE ROW LEVEL SECURITY;
CREATE POLICY branches_project_isolation ON branches FOR ALL
  USING (project_id = auth_project_id() OR auth_is_superadmin())
  WITH CHECK (project_id = auth_project_id() OR auth_is_superadmin());
```
**تحقق:** `SELECT count(*) FROM loans; SELECT count(*) FROM branches;` (يُشغَّل بـSQL Editor، يعمل بصلاحية postgres فيتجاوز RLS — للتحقق من عدم كسر البنية فقط). التحقق الحقيقي: **افتح مزاهر وتصفّح كل الصفحات** — لا صفحة تستعلم هذين الجدولين، فلا يوجد أي فرق ظاهر متوقع. هذا بالضبط ما يجعلهما آمنين للتجربة: صفر تأثير وظيفي، واختبار حقيقي لآلية `ENABLE`/`DISABLE` والدوال المساعدة قبل الجداول الحقيقية.
**تراجع فوري:**
```sql
ALTER TABLE loans DISABLE ROW LEVEL SECURITY;
ALTER TABLE branches DISABLE ROW LEVEL SECURITY;
```

### 2-3 حتى 2-13 — الـ11 جدولاً "العاديين" — **جاهزة SQL، لكن محظورة التنفيذ حتى يُحل القسم 3**

نفس القالب حرفياً لكل جدول (`<T>` = اسم الجدول):
```sql
ALTER TABLE <T> ENABLE ROW LEVEL SECURITY;
CREATE POLICY <T>_project_isolation ON <T> FOR ALL
  USING (project_id = auth_project_id() OR auth_is_superadmin())
  WITH CHECK (project_id = auth_project_id() OR auth_is_superadmin());
```
القائمة بالترتيب المعتمد بالقسم 1: `supplier_transactions` · `suppliers` · `payable_suppliers` · `document_items` · `attachments` · `quick_sales_draft` · `categories` · `sales` · `project_settings` · `documents` · `ledger_entries`.

**تحقق لكل جدول (بعد أن يُحل القسم 3):** سجّل دخول بكل دور يلمس الجدول (انظر عمود "الأدوار" بالقسم 1) وتأكد أن صفحاته تعرض البيانات وتحفظ التعديلات بلا نقص.
**تراجع فوري لأي جدول:** `ALTER TABLE <T> DISABLE ROW LEVEL SECURITY;`

**سبب تجميعها هنا معاً بدل تفصيل جدول-جدول:** القالب متطابق تماماً للـ11 كلها — لا فائدة تقنية من تكرار نفس 3 الأسطر 11 مرة. الفارق الحقيقي بينها هو **ترتيب التنفيذ** (القسم 1) لا **شكل السياسة**.

### 2-14/2-15. `projects` و`app_users` — ⚫ تصميم خاص (تفصيل بالقسم 3-جـ)

لا أكتب SQL نهائياً لهذين الآن — القسم 3-جـ يشرح لماذا نمط `auth.uid()` القياسي لا يكفي لهما، ويعرض المسار الموصى به.

---

## 3. المعضلة الانتقالية — الأهم، كما طلبت بالضبط

### 3-أ. حجم المشكلة بالأرقام
- **1 من 5 حسابات** فقط له `auth.uid()` حقيقي (المالك).
- **11 من 15 جدولاً** تُلمس يومياً بأدوار PIN-only (accountant/cashier/purchasing).
- تفعيل `auth.uid()`-only على أي منها الآن = **تعطيل كامل وفوري**، لا تدهور تدريجي.

### 3-ب. هل يحتاج حلاً مؤقتاً؟ — نعم، حتماً. وهنا خياران فعليان

**الخيار A — نفس آلية المرحلة 1 لكل الأدوار (المُوصى به):**
امنح accountant وcashier وpurchasing جلسة Supabase حقيقية أيضاً، **دون تغيير تجربة PIN الظاهرة للمستخدم**. آلية عملية:
1. لكل صف بـ`app_users` (غير owner)، يُنشأ حساب Supabase Auth بإيميل اصطناعي غير قابل للتخمين (مثل `accountant-{project_id}@internal.tahseeb.app`) وكلمة مرور عشوائية طويلة **لا يعرفها أحد ولا تُخزَّن بالواجهة**.
2. عند نجاح `login(pin)` الحالي (نفس التحقق الحالي بالضبط، بلا تغيير)، تستدعي الواجهة **دالة خادمية جديدة** (Vercel Function بمفتاح `service_role`، تشبه بنية `api/_guard.js` الموجودة) تُصدر جلسة Supabase لذاك الحساب الاصطناعي (عبر `supabase.auth.admin.generateLink` أو ما يعادلها) وتُعيدها للمتصفح، الذي يضبطها بـ`supabase.auth.setSession()`.
3. **النتيجة:** المستخدم يكتب PIN كما هو تماماً بلا أي تغيير بالشاشة، لكن خلف الكواليس تصير كل طلباته بدور `authenticated` مع `auth.uid()` حقيقي — فتعمل سياسات القسم 2 بلا استثناء.

هذا **يتطلب كوداً جديداً** (دالة خادمية + تعديل بسيط بـ`login()`) — خارج نطاق "SQL فقط"، لكنه **الحل الوحيد الذي يُصلح الثغرة فعلياً** لهذه الأدوار الثلاثة. أقترح تسميته **"المرحلة 2-أ"** — خطوة تحضيرية داخل المرحلة 2، تحتاج موافقة وتخطيطاً منفصلاً (بنفس مستوى الدقة الذي طلبته هنا) قبل أي كود.

**الخيار B — سياسة انتقالية "بوابة مزدوجة" (بديل أسرع، لكن وهمي أمنياً):**
```sql
USING (
  (project_id = auth_project_id() OR auth_is_superadmin())  -- المسار الحقيقي (المالك اليوم)
  OR auth.role() = 'anon'                                    -- ⚠️ استثناء مؤقت — يبقي PIN يعمل
)
```
هذا **يُشغِّل** RLS تقنياً بلا كسر أي دور، لكن **لا يضيف أي حماية فعلية** ضد الزائر المجهول — لأن مفتاح `anon` نفسه (المكشوف بحزمة JS) يمنح دور `anon`، فأي شخص يقرأ الحزمة يمرّ من نفس الثغرة الحالية بالضبط. الفرق الوحيد: يثبت صحة السياسات وسلوك `ENABLE`/`DISABLE` قبل الجدولين الحقيقيين. **لا أوصي به كحل — فقط كسقالة اختبار مؤقتة إن أردت البدء بالجداول الفارغة الخمسة (Tier 0+1) بلا انتظار المرحلة 2-أ**، مع وضوح تام أن الثغرة الأصلية (زائر مجهول يقرأ كل شيء) **تبقى مفتوحة بالكامل** حتى تُزال هذي البوابة لاحقاً.

### 3-جـ. `app_users` و`projects` — مشكلة إضافية فوق ما سبق
حتى بعد المرحلة 2-أ (كل الأدوار لها `auth.uid()`)، يبقى تسجيل الدخول **نفسه** يحتاج قراءة `app_users` (مطابقة PIN) و`projects` (مطابقة subdomain) **بدور anon قبل تأسيس أي جلسة** — هذي مرحلة "قبل تسجيل الدخول"، لا يمكن أن تشترط `auth.uid()` بحكم التعريف (لا جلسة بعد).

القراءة المباشرة الحالية (`SELECT * ... WHERE pin = X`) خطرة أصلاً بمعزل عن RLS: عمود `pin` نصي صريح (5 أرقام = 100,000 احتمال) مكشوف بالكامل لأي طالع بمفتاح anon — كما وثّق `review-2-security.md`. **RLS بنمط الصف الواحد لا يحل هذا** (يُحدَّد بالصف لا بشكل الاستعلام)، فحتى "سياسة تسمح فقط بمطابقة PIN صف واحد" لا تمنع تعداد كل الأرقام الممكنة بمفتاح anon العام.

**الحل الصحيح لهذين الجدولين تحديداً:** نقل التحقق لدالة `SECURITY DEFINER` واحدة (`login_with_pin(subdomain, pin)`) تُعيد فقط `{name, role, project_id, branch}` عند تطابق، ثم **سحب صلاحية القراءة المباشرة** من `anon` على `app_users`/`projects` (`REVOKE SELECT ... FROM anon`) بحيث المسار الوحيد هو الدالة. هذا يستدعي تعديل `AuthContext.jsx` (`.from('app_users').select()` → `.rpc('login_with_pin', {...})`) — تصميم مستقل يستحق خطته الخاصة، ويجب أن يأتي **بعد** المرحلة 2-أ لا قبلها.

---

## 4. الجدول الزمني الواقعي

**بناءً على القسم 3 — الجواب الصادق: لا يمكن تفعيل RLS الحقيقي على 13 من 15 جدولاً بجلسة أو جلستين. المرحلة 2 كاملة تحتاج تسلسلاً من 3 دفعات، لا "جلسة واحدة مع اختبار بعد كل جدول" كما كان مأمولاً قبل هذا الاكتشاف:**

| الدفعة | ماذا | الشرط المسبق | الجلسات المقترحة |
|---|---|---|---|
| **1** | `loans` + `branches` (Tier 0) | لا شيء — جاهزة الآن | جلسة واحدة قصيرة. صفر مخاطرة، صفر أثر وظيفي |
| **2-أ** *(تخطيط منفصل مطلوب)* | دالة خادمية + جلسة Supabase تلقائية لـaccountant/cashier/purchasing خلف PIN | موافقتك على خطة مستقلة بنفس هذا المستوى من التدقيق | 2-3 جلسات (بناء + اختبار كل دور على حدة) |
| **3** | الـ11 جدولاً "العاديين" (القسم 2، بالترتيب المعتمد) | نجاح الدفعة 2-أ لكل الأدوار الأربعة | جلسة لكل 2-3 جداول متقاربة الخطورة، مع اختبار الأدوار اللامسة بعد كل جدول — تقديراً: 4-5 جلسات |
| **4** *(تخطيط منفصل مطلوب)* | `app_users` + `projects` عبر دالة `login_with_pin` + سحب صلاحيات anon | نجاح الدفعة 3 كاملة | جلسة تصميم + جلسة تنفيذ منفصلتان — الأخطر، يستحق نفس معاملة التدقيق التي طلبتها هنا |

**لا أوصي بـ"جلسة واحدة لكل الـ15"** — ليس فقط للحذر، بل لأنها **مستحيلة تقنياً** بأمان حقيقي قبل الدفعة 2-أ.
**أوصي بالبدء الآن بالدفعة 1** (صفر مخاطرة، يبني الثقة بآلية `ENABLE`/`DISABLE` والدوال المساعدة)، **بالتوازي** مع تخطيط الدفعة 2-أ كخطوة منفصلة تالية — بما أنها الحاجز الحقيقي أمام كل شيء آخر.

---

## القرار المطلوب منك الآن
1. ✅ الموافقة على تفعيل **Tier 0 فقط** (`loans` + `branches`) بالسياسات أعلاه — جاهزة للتنفيذ بمجرد إذنك، منفصلة عن أي شيء آخر.
2. توجيه بخصوص القسم 3-ب: هل نخطط للخيار A (المرحلة 2-أ — الحل الحقيقي، يحتاج كوداً) الآن، أم تريد تفاصيل إضافية أولاً؟

**لم يُنفَّذ أي SQL ولم يتغيّر أي كود أثناء إعداد هذه الخطة.**
