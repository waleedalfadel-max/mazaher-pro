# المراجعة 5 — إغلاق فجوة "الكود المربوط باسم مشروع" (نهائي)

> تشخيص فقط. لا تعديل نُفِّذ. فحص كامل لـ`src/` بعد كل تعديلات اليوم (الدفعة 1، الدفعة 2، أمن Storage، إصلاح الجلسات).

---

## 1) النتيجة — خبر جيد: لم يتبقَّ إلا موضعان، وكلاهما مستثنى مسبقاً

بحثت بحثاً شاملاً بكل `src/` عن أي مقارنة نصية مباشرة باسم مشروع (`'ديوانية مزاهر'` وما شابهها) أو أي `projectName === `/`projectName?.includes(` غير مغطى بنظام `modules`/`project_settings` الديناميكي الموجود أصلاً:

```
grep -rn "ديوانية مزاهر" src/
grep -rn "projectName\s*===\|projectName?\.includes\|projName\s*===" src/
```

**النتيجة الكاملة — موضعان فقط:**

| # | الملف : السطر | الشرط | التصنيف |
|---|---|---|---|
| 1 | [Layout.jsx:39](src/components/Layout.jsx#L39) | `cond: n => n === 'ديوانية مزاهر'` — يظهر رابط "إدخال سريع للمبيعات" | ⛔ **مستثنى** |
| 2 | [CashierDashboard.jsx:147-148](src/pages/CashierDashboard.jsx#L147) | `role === 'cashier' && projectName === 'ديوانية مزاهر'` — تنبيه بديل POS | ⛔ **مستثنى** |

كلا الموضعين لنفس الميزة الواحدة (**الإدخال السريع للمبيعات / Quick Sale**) — وهذي هي الميزة التي ألغيت تحويلها لـfeature flag نهائياً بقرارك الصريح سابقاً بهذي الجلسة:

> *"ألغينا خطة 'دفعة 3' (تحويل الإدخال السريع للمبيعات إلى feature flag) نهائياً — ... اترك تلك الميزة كما هي بالضبط بلا أي تعديل."*

**لذلك: لا أقترح أي تغيير عليهما بهذا التقرير، ولن يظهرا بأي خطة تنفيذ لاحقة، إلا إن غيّرت قرارك صراحة.** أذكرهما هنا فقط للاكتمال — نفس مستوى الشفافية المتبع طوال اليوم (لا إخفاء نتيجة حتى لو كانت "مستبعدة سلفاً").

**كل شرط آخر كان مربوطاً باسم مشروع محذوف (تشورميك، كون، بـ عسل، تيست مزاهر) — حُذف بالكامل بالدفعة 2 اليوم.** لا بقايا. البحث الشامل أعلاه يؤكد ذلك بنفسه (صفر نتائج إضافية).

---

## 2) فجوة معمارية حقيقية — لكنها ليست "شرط سلوك"، بل بيانات

أثناء البحث وجدت شيئاً مختلفاً عن نمط "الشروط الميتة" الذي عالجته الدفعة 2: [Login.jsx:12-18](src/pages/Login.jsx#L12) — خريطة `PROJECT_INFO` الثابتة بالكود:

```js
const PROJECT_INFO = {
  mazaher: {
    name: 'ديوانية مزاهر',
    welcome: 'مرحباً بك في نظام المتابعة المالية',
    color: '#6EB7B0',
  },
}
```

هذا **ليس** شرط ميزة (feature flag) — بل بيانات علامة تجارية تُعرَض بشاشة الدخول *قبل* أي مصادقة. لكنه يقع بالضبط ضمن ما طلبته بالبند 3: **"أي مشروع جديد يُفعِّل ميزة بإضافة قيمة لقاعدة البيانات، بدون لمس كود إطلاقاً"** — اليوم، إضافة عميل جديد يتطلب حتماً:
1. إضافة صف بجدول `projects` (subdomain جديد) — بيانات، لا مشكلة.
2. **تعديل كود `Login.jsx` وإعادة نشر** لإضافة مدخل جديد بـ`PROJECT_INFO` — وإلا ستظهر شاشة دخول بلا اسم/ترحيب/لون مخصَّص لذلك العميل.

### لماذا كُتبت هكذا أصلاً — وليست مجرد إهمال
تحقّقت من السبب قبل اقتراح أي حل: شاشة الدخول تُستدعى **قبل تسجيل الدخول تماماً** — لا جلسة، لا `auth.uid()`. لو حاولت قراءة `projects` مباشرة بالمتصفح كـ`anon`، ستُحجَب بواسطة RLS (نفس السبب الذي دفعنا لبناء `login_with_pin` كدالة `SECURITY DEFINER` سابقاً هذي الجلسة). لذلك كانت خريطة ثابتة بالكود هي الحل العملي الوحيد وقتها — **قبل** أن يكون لدينا نمط "دالة RPC ضيّقة تكشف الحد الأدنى الآمن فقط" جاهزاً ومُختبَراً (وهو الآن موجود ومُثبَت النجاح عبر `login_with_pin`).

---

## 3) تصميم نظام Feature Flags النهائي

بما أن الكود شبه خالٍ تماماً من الشروط السلوكية المربوطة بأسماء مشاريع (الفجوة الوحيدة المتبقية هي بيانات علامة تجارية، لا سلوك)، التصميم أدناه له هدفان منفصلان:

### أ) توحيد ما هو موجود فعلاً اليوم (لا تغيير سلوك، توثيق فقط)
النظام الديناميكي **موجود وناجح جزئياً** بالفعل — استخدمته الدفعة الحالية كمرجع:

| الآلية | أين تُقرأ | أين تُحرَّر اليوم | مثال حي |
|---|---|---|---|
| `project_settings.settings.modules[]` | [Layout.jsx:55](src/components/Layout.jsx#L55) عبر `getProjectModules()` | ❌ لا واجهة تحرير بعد | رابط "الموردين" (`module: 'suppliers'`) |
| `project_settings.settings.branches[]` | `Reports.jsx`, `PendingDocuments.jsx` | ❌ لا واجهة تحرير بعد | قائمة الفروع |
| `project_settings.settings.transaction_types[]` | `CashierDashboard.jsx`, عدة صفحات | ✅ `SuperAdmin.jsx` (`SettingsSection`) | أنواع الحركات |
| `project_settings.active` | `SuperAdmin.jsx` | ✅ زر تفعيل/تعطيل | تعطيل مشروع كامل |

هذا **هو** نظام الـfeature flags بالفعل — لم يكن يحتاج اختراعاً من الصفر، فقط **توسيعاً** بمفتاح عام جديد للمستقبل، ووصفاً موحَّداً.

### ب) إضافة مفتاح عام مستقبلي — `settings.flags{}`
لأي ميزة قادمة شبيهة بـQuick Sale (خاصة بمشروع واحد، منطق عمل فريد لا يستحق تعميمه)، بدل مقارنة اسم:

```json
{
  "modules": ["suppliers"],
  "branches": ["الفرع الرئيسي"],
  "transaction_types": [...],
  "flags": {
    "quick_sale": false
  }
}
```

نمط القراءة بالكود (مثال توضيحي فقط — **لا يُطبَّق على Quick Sale الآن**، هذا فقط يُثبت أن أي ميزة *مستقبلية* مشابهة ستُبنى بهذا الشكل من أول يوم بدل مقارنة اسم):
```js
cond: (n, flags) => flags?.quick_sale === true
```

### ج) حل فجوة Login.jsx — دالة RPC ضيّقة، بنفس نمط `login_with_pin`
```sql
create or replace function get_project_branding(p_subdomain text)
returns table (name text, welcome text, color text)
language sql
security definer
set search_path = public
stable
as $$
  select p.name,
         coalesce(ps.settings->>'welcome_message', 'مرحباً بك في نظام المتابعة المالية'),
         coalesce(ps.settings->>'brand_color', '#6EB7B0')
  from projects p
  left join project_settings ps on ps.project_id = p.id
  where p.subdomain = p_subdomain
  limit 1;
$$;
```
تكشف **فقط** الحقول الثلاثة الآمنة للعرض قبل الدخول — لا شيء آخر من `projects` (لا `id` حتى، غير ضروري هنا). مطابقة تماماً لمبدأ `login_with_pin` (أضيق كشف ممكن، لا معلومات زائدة).

`Login.jsx` يستدعيها بدل قراءة `PROJECT_INFO[subdomain]` مباشرة، مع **fallback** لو فشل الاستدعاء (لا إنترنت مؤقت، إلخ) → نص افتراضي عام "مرحباً بك" بلا تعطيل شاشة الدخول كلياً.

---

## 4) خطة تنفيذ تدريجية — خفيفة، لأن المتبقي فعلياً قليل جداً

### الدفعة أ — SQL فقط (تحتاج تشغيلك، كالعادة — لا أملك تنفيذ SQL)
1. `create or replace function get_project_branding(...)` أعلاه.
2. عمودان اختياريان جديدان (أو حقول بـ`project_settings.settings` بدل أعمدة جديدة بـ`projects` — أرجّح الخيار الثاني: أبسط، لا `ALTER TABLE` على `projects`، يتّسق مع نمط `transaction_types`/`branches` الحالي القائم على `settings` jsonb).
3. **تحقق آلي بعدها (أنفّذه أنا):** استدعاء الدالة مباشرة عبر PostgREST RPC بمفتاح anon (`supabase.rpc('get_project_branding', {p_subdomain:'mazaher'})`) من سكربت تحقّق مؤقت، للتأكد من إرجاع `{name:'ديوانية مزاهر', welcome:'...', color:'#6EB7B0'}` بلا الحاجة لأي جلسة أو دخولك الفعلي.

### الدفعة ب — كود `Login.jsx` (أنفّذه أنا بعد موافقتك على الدفعة أ)
استبدال `PROJECT_INFO[subdomain]` باستدعاء `get_project_branding` + fallback نصي عام. `npm run build`، ثم فحص حي بلا جلسة (Browser tool) لكل subdomain حالي (`mazaher` فقط اليوم) للتأكد من ظهور نفس الاسم/الترحيب/اللون الحاليين تماماً — بلا أي فرق ملحوظ للمستخدم.

### الدفعة ج — توثيق `settings.flags{}` (بلا أي كود يُنفَّذ اليوم)
مجرد إضافة الاصطلاح لملف التوثيق الداخلي (أو تعليق بـ`lib/projectSettings.js`) ليكون جاهزاً متى احتجناه فعلياً — **صفر تغيير سلوكي**، فقط اتفاق تسمية لأي مطوّر/جلسة مستقبلية.

**لا توجد دفعة (د) أو أبعد — هذا كل ما تبقّى فعلياً بعد عمل اليوم.**

---

## 5) خطة الاختبار — كل ما هو آلي، أنفّذه بنفسي

| بعد أي دفعة | ماذا أتحقق منه أنا آلياً |
|---|---|
| الدفعة أ (SQL) | استدعاء `get_project_branding('mazaher')` مباشرة عبر anon RPC — يُرجع القيم الصحيحة، لا حاجة لجلسة |
| الدفعة ب (كود) | `npm run build` نظيف؛ فتح `mazaher.tahseeb.app` بمتصفح بلا جلسة (Browser tool) والتحقق أن شاشة الدخول تعرض بالضبط نفس الاسم/الترحيب/اللون كما اليوم؛ فحص Console بلا أخطاء |
| الدفعة ج | لا اختبار مطلوب (توثيق فقط) |

**الشيء الوحيد الذي يحتاجك فعلياً بكامل هذي الخطة: تشغيل SQL الدفعة أ بمحرر Supabase** (لا بديل تقني — لا أملك وصول SQL كما هو معروف طوال اليوم). لا حاجة لأي دخول حقيقي بحساباتك، ولا أي اختبار يدوي آخر — كل شيء غير ذلك قابل للتحقق الآلي البحت.

---

## الخلاصة
- **موضعان فقط متبقيان بالكود مربوطان باسم مشروع — كلاهما Quick Sale، مستثنيان بقرارك السابق النهائي، لن يُمسّا.**
- **فجوة حقيقية واحدة**: شاشة الدخول تتطلب تعديل كود لأي عميل جديد — الحل بدالة RPC ضيّقة على نفس نمط `login_with_pin` المُثبَت نجاحه اليوم.
- خطة التنفيذ خفيفة عمداً (دفعتان فعليتان + توثيق) لأن عمل اليوم (الدفعتين 1 و2) أنجز الجزء الأكبر فعلاً.

**لم يُنفَّذ أي تعديل. بانتظار مراجعتك وموافقتك.**
